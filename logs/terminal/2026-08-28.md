# 2026-08-28

## Разбор "*" перед синком

- Проверен старый случай (скриншот `materials/la.jpeg`, слово عَلَىٰ без
  верного варианта в тренажёре) — подтверждено: уже исправлен 19.08.2026
  (`_merge_glued_translations`).
- Пользователь спросил: нужно ли то же расклеивание на странице чтения
  мусхафа (тап-попап, не квиз)? Разбор показал — прямой ответ "нет" был бы
  неверным: страница чтения брала перевод из той же `mufradat_words` без
  фильтра `_is_junk`, и живой баг реально был — 2222 слова-хвоста склейки
  показывали буквальный `"*"` в попапе (проверено на всех 604 уже
  сгенерированных `page*.json`, не гипотетически).
- Решение (advisor): не переносить перевод головы на хвост (создало бы
  зомби-запись в "Мои слова" — progress_key хвоста резолвится в мусор),
  просто делать хвост нетапабельным, как обычное непереведённое слово.

## Фиксы и деплой

1. `scripts/export_mushaf_page.py:get_ayah_words` — `_is_junk` фильтрует
   перевод при экспорте. `core/mufradat_api.py:handle_words_add` — та же
   защита от старого закэшированного клиента, который может ещё прислать
   `"*"`.
2. Все 604 `mushaf_data/page*.json` пропатчены на месте (2302 токена
   `"*"` → пустой перевод) — и локально, и на сервере (scp в staging +
   `sudo -u stursunkul cp`). Проверено: 0 `"*"` осталось в обоих местах.
3. Шапка вкладки "Мои слова" — раньше показывала ambient-имя суры
   (неочевидно с телефона, заметил пользователь), теперь "🔖 Мои слова".
4. Коммиты `20aea13`, `f8a7ec4`, `5d1d942` — задеплоены на оба бота
   (мужской автодеплоем, женский вручную `systemctl --user restart`).

## Синк mufradat_words на весь Коран (главная задача дня)

- Перед синком: систематический разбор 4 пропущенных аятов (2:181, 8:6,
  13:37, 37:130 — расхождение счёта слов API/`quran_transcript` на
  устойчивых сочетаниях). Реконсилированы вручную, сохранены в
  `scripts/fix_skipped_ayat.py` (идемпотентно, безопасно перезапускать).
- Advisor: НЕ копировать файл БД (перезаписал бы прогресс 11097 студентов,
  id/progress_key могли разъехаться) — запускать `ingest_mufradat.py`
  прямо на сервере (UPDATE на месте, безопасно).
- Бэкап прод-БД перед стартом (`hadiths.db.bak-20260828-pre-mufradat-sync`).
- `ingest_mufradat.py` запущен на сервере в фоне (nohup), ~24 минуты,
  через Monitor-мониторинг прогресса по сурам. Результат: 77383 слова
  сохранено, суры 1-114, 0 HTTP-ошибок, ровно те же 4 аята пропущены
  (ожидаемо). Следом `fix_skipped_ayat.py` — +50 слов.
- Финальная проверка: 77433 ru-строк (77383+50, совпадает с независимым
  подсчётом на локальной базе), 0 дублей, `mufradat_progress` 11097→11204
  и `mushaf_starred_words` 1→2 — рост за счёт реальных живых студентов за
  время синка, не сбой. id/progress_key на контрольной строке (2:3:1 ru)
  стабильны. Смоук-тест: `get_words_in_range(50, 1, 5, 'ru')` отдаёт 39
  чистых слов (сура Каф, раньше не покрывалась вообще).
- Устаревшие комментарии "суры 2-10" в `core/mushaf_words.py`,
  `core/mufradat.py`, `core/mufradat_api.py` убраны (коммит `5d1d942`).

## Живая проверка после деплоя (28.08, день использования)

- Мусхаф: 7 уникальных IP, 7 разных страниц открыто.
- Тренажёр: 6 уникальных IP, 220 запросов к API, 247 отвеченных слов.
- "Мои слова": 3 записи от 2 студентов, все добавлены сегодня, переводы
  чистые (без "*") — фича используется и работает штатно.

## Итог (первая половина сессии)

- Все три задачи (шапка "Мои слова" → "*"-фикс → полный синк Корана)
  выполнены по порядку, с подтверждением пользователя на каждый шаг.

## Вторая половина сессии — "Мои слова" при ошибке + навигация мусхафа

### Неверный ответ → авто-добавление в "Мои слова"

- Симметрично авто-удалению: любая ошибка в тренажёре закидывает слово в
  список, без предела размера (решение пользователя после вопроса
  advisor'а про риск "журнала ошибок").
- Развязка "выход из списка" (ручной ИЛИ 5 верных подряд) от "уход на
  60-дневный отдых" — оба пути теперь сбрасывают `correct_streak` в 0,
  слово заново проходит путь "как обычное". Advisor снял тревогу про
  просадку "Общего веса" — на пуле ~1161 слово незаметно (~0.01/10).
- Таджвид-цвет убран из "Мои слова" везде, включая 2 живые записи на
  проде (почищены вручную).
- Коммит `c028f28`, 73/73 теста, оба бота обновлены.

### Закладка чтения + навигация — урок по RTL

- Добавлены кнопки: 🔖 (сохранить закладку чтения, на сервере по Telegram
  ID), « (перейти к закладке), » (начало Аль-Бакара, стр. 2). Коммит `2a8fdd3`.
- **После деплоя пользователь сообщил, что кнопки "ведут не туда" и "стоят
  не на том краю".** Несколько раундов перестановки DOM (4-5 коммитов)
  НЕ помогали — путаница только росла, пользователь справедливо
  раздражился ("такая простая задача полчаса возимся").
- Вызван advisor. Найдена настоящая причина: `#page-nav` наследует
  `dir="rtl"` от `<html>` — ОДНОВРЕМЕННО переворачивает визуальный порядок
  элементов И зеркалит сами символы `«`/`»` (Bidi_Mirrored юникод-символы).
  У соседних `‹`/`›` оба эффекта случайно взаимно гасились, поэтому баг
  никогда не проявлялся там же. Код был правильным всё время, отображение
  — нет.
- **Методологический урок**: измерение ширин/координат элементов (что
  делалось несколько раз до этого) НЕ ловит этот класс бага — нужен
  реальный скриншот (`Page.captureScreenshot` headless Chrome), чтобы
  увидеть форму отрисованного глифа, не только его позицию.
- Фикс: `direction: ltr` на новых кнопках + DOM-порядок в обратную сторону
  от желаемой картинки. Подтверждено скриншотом (не на словах). Финальный
  порядок на экране: `🔖 « ‹ стр. N › »`. Коммит `89eee40`.
- Заодно развёрнуто направление свайпа по просьбе пользователя (было
  "как переворот бумажной книги", показалось неинтуитивным). Коммит `204febc`.
- Записано в `wiki/mushaf_yassirapp.md` как правило на будущее: любой
  новый направленный символ в RTL-контейнере — либо свой `direction: ltr`,
  либо проверка скриншотом ДО показа результата пользователю.

## Итог дня (первые две части)

- Открытых задач по обеим темам не осталось. Все правки задеплоены на оба
  бота (статика `mushaf_data/index.html` — без рестарта; изменения в
  `core/*.py` — с рестартом обоих).

## Третья часть — анонс, повторение, онлайн

### Анонс YassirApp в группу

- Составлен и отправлен текст-объяснение фич (Мусхаф/Тренажёр/связка) в
  группу "Yassir Тадаббур" по прямой просьбе пользователя. Поймал сам себя
  до отправки: черновик был с `**жирным**` в GitHub-стиле, а
  `core/tg.py:_raw_send` не ставит `parse_mode` вообще - переписал в
  чистый текст с эмодзи-заголовками перед реальной отправкой.

### Кнопка "🔁 повторение"

- Студент, перечитавший от начала Аль-Бакары до места, где остановился,
  сам подтверждает это (доверие, без проверки факта) - тап на 🔁 в шапке
  → подтверждение "Вы сделали повторение...?" → "Да" → зачёт задания "r"
  + сообщение в группу "Имя, повторение + (через YassirApp, Мусхаф)."
  (формулировка источника — по просьбе пользователя).
- Переиспользована существующая инфраструктура: `save_report`/
  `get_today_report` (та же, что у текстовой сдачи в группе,
  core/handlers.py), паттерн взят из уже существующего
  `_credit_task_if_applicable` (зачёт задания "Слова" через тренажёр).
  Новая `credit_revision_task` в core/mufradat_bot.py.
- Место в UI решали вместе с пользователем: `#topbar` (не `#page-nav`,
  тот уже переполнен и там же был RTL-баг) - осознанный выбор оказался
  верным, RTL-проблем на этот раз не было (пока не добавили новый
  элемент - см. ниже).
- 4 новых теста (tests/test_mushaf_revision_credit.py) через `test_db`
  фикстуру (изолированная временная БД) - идемпотентность, отсутствие
  задания "r" в группе, неизвестный пользователь.

### "Сейчас онлайн"

- Простая приближённая метрика присутствия - heartbeat с фронтенда каждые
  20 сек, сервер держит `{user_id: last_seen}` в памяти процесса
  (`core/mufradat_api.py`), "онлайн" = отметился за последние 60 сек.
  Раздельно по боту само собой (свой процесс на мужской/женский).
- Не показывать при <3 - по решению пользователя после первого же вопроса
  (не хотели, чтобы "1-2 онлайн" выглядело бедно).
- **Та же RTL-ловушка, что и на навигации мусхафа, проявилась СНОВА** -
  новый `#dash-online` тоже наследует `dir="rtl"` от `<html>`, порядок
  "точка-число-текст" отобразился задом наперёд при первой же проверке.
  На этот раз поймано СРАЗУ скриншотом headless Chrome, ДО показа
  пользователю - урок из предыдущей части сессии применён на практике,
  не повторили 30-минутную путаницу.

### Разное

- Разъяснено (не код): заголовок Mini App "Yassir assistant" — имя бота
  в BotFather, не наша страница, меняется пользователем через `/setname`.

## Итог дня (финал)

- Все три подсессии дня закрыты, открытых задач нет. Полный список
  коммитов дня (в порядке): `20aea13`, `f8a7ec4`, `5d1d942`, `ea97429`,
  `c028f28`, `2a8fdd3`, `029e381`, `85dca3a`, `73d164b`, `13c005d`,
  `e6d7d60`, `89eee40`, `204febc`, `8d9edbb`, `38ebae6`, `e53da21`,
  `9c5a241`, `92f19fd`, `67af614`.

## Четвёртая часть — навигация, тема, контекстная панель, разбор шрифта

### Прямая навигация Мусхаф↔Тренажёр

- Кнопка "🌱" в мусхафе → `openTrainer()` напрямую. В тренажёре новая
  нижняя панель ("🔖 Мои слова"/"📗 Мусхаф") → `openMushaf(tab)`, общая
  функция, вынесенная из `dash-mushaf`. Коммит `5ac818c`.

### Переключатель темы

- Прямой вопрос студента "можно выбрать тему?" → реализовано: 🌙/☀️ на
  дашборде, `localStorage`, ручной выбор перебивает системную тему в
  обе стороны. Применяется отдельным скриптом в `<head>` ДО первой
  отрисовки - без вспышки неправильной темы. Проверено скриншотами
  (light→dark клик, dark сохраняется после перезагрузки).

### Контекстная нижняя панель мусхафа — несколько раундов уточнений

- Началось с "убери иконку закладки, зачем иконка страницы, останется
  2 иконки" → через серию уточнений пользователя пришли к финальной
  схеме (панель показывает "куда пойти", никогда "где ты сейчас"):
  - Страница: Смысл, Мои слова, Тренажёр (3, текст+иконка)
  - Смысл: только "🔙" (в Смысл только один вход - со Страницы)
  - Мои слова: Тренажёр + "🔙", а "🔙" **умный** - если в "Мои слова"
    зашли со Страницы, ведёт на Страницу; если из Тренажёра - ведёт в
    Тренажёр (`mywordsOrigin`, `openMushaf(tab, mywordsFrom)`).
- Перед реализацией явно попросил "дай полную картинку я подтвержу" -
  собрал таблицей все 3 состояния, дождался "все верно", только потом
  писал код. Полностью проверено headless-скриншотами каждого состояния
  (Страница/Смысл/Мои слова×2 origin) + кликами по цепочке до реализации,
  не после.
- Заодно `#app` получил тот же фоновый градиент, что `#trainer`/
  `#dashboard` (по просьбе "посмотреть как будет выглядеть"). Коммит
  `d724908` (тема+панель+фон одним коммитом).

### Разбор "стр. 7 мельче стр. 6"

- Headless Chrome НЕ воспроизвёл разницу (обе страницы 1.000 ratio, без
  сжатия, на 360/390/414px) - синтетический тест не совпал с реальным
  устройством пользователя (другой движок рендеринга шрифта).
- Пользователь скинул реальные скриншоты (`materials/6.jpeg`,
  `7.jpeg`) - сравнение подтвердило разницу визуально: у стр. 7 самая
  длинная строка объективно длиннее/плотнее, чем у стр. 6 (реальное
  свойство построчной вёрстки настоящего печатного мусхафа). Вывод: не
  баг, ожидаемый побочный эффект точной построчной вёрстки - `clampFontToFit`
  ужимает КАЖДУЮ страницу под её собственный худший случай.
- **Методологический урок**: синтетический headless-тест не заменяет
  реальный скриншот с устройства, когда расхождение может быть в самом
  рендеринге шрифта/движке браузера, а не в логике кода.

## Итог дня (после четвёртой части)

- Все четыре части сессии закрыты. Финальные коммиты: `5ac818c`,
  `d724908`.

## Пятая часть — палитра мусхафа, disambiguation, чистка переводов, критическая находка

### Дубль-кнопка и дизайн через frontend-design

- Убрана дублирующая "Тренажёр" на "Мои словах" при заходе из Тренажёра
  (обе кнопки вели в одно место) - коммит `6b91d14`.
- Пользователь спросил "через скилл подобрал?" про цвет chrome-фона -
  честно ответил "нет, на глаз", прогнал через `frontend-design` заново
  (`color-mix` вместо чистого белого) - коммит `ad4d232`.

### Отдельная палитра мусхафа

- Пользователь: "цвет мусхафа теплее, айвери/бежевый... но можешь
  что-то другое подобрать, приятное для чтения Великой Книги" - явно
  дал творческий простор, не жёсткое ТЗ на конкретный hex. Прогнал
  через `frontend-design` с полным брифом (палитра проекта, таджвид в
  самом шрифте, что должно ХОРОШО читаться на новом фоне).
- Завёл ОТДЕЛЬНЫЕ токены `--mushaf-bg-start/-end`, не трогая
  `--dash-bg-*` (те остаются только у тренажёра/дашборда - прямое
  требование). Светлая `#fbf8f0→#f4ecd8` (тёплая бумага), тёмная
  `#201a12→#17130d` (переплёт книги, не зеленовато-чёрный тренажёра).
  Золотая подсветка (`.icon-gold`) на закладке/"Мои слова" -
  тематическая связь с `--marker-color` (номера-маркеры аятов).
- Проверено скриншотами во ВСЕХ 4 комбинациях (мусхаф/дашборд ×
  светлая/тёмная) ДО показа пользователю - дашборд/тренажёр
  подтверждённо не тронуты пиксель в пиксель. Коммит `1411ceb`.
- Пользователь сразу поймал: "почему иконка закладки и Мои слова
  одинаковые?" - оба 🔖 плюс одинаковая золотая подсветка выглядели как
  одна функция. Заменил "Мои слова" на ⭐ (совпадает с реальным именем
  таблицы БД `mushaf_starred_words`) - коммит `8b4c19c`.

### Чистка переводов от висящих знаков препинания

- Пользователь заметил запятые/точки/тире в конце отдельных слов в
  тренажёре и "Мои словах", спросил "мы же не потеряем что-то?" -
  разобрал на РЕАЛЬНЫХ данных перед тем как трогать код (8928 слов на
  запятой, 5206 на точке, 1714 на двоеточии, 191 на точке с запятой,
  ~1740 на тире с пробелом перед ним - из 77433 строк). Нашёл 6 случаев,
  где тире БЕЗ пробела перед ним - НЕ мусор, а слово, разрубленное API
  пополам ("из-" от "из-за", "Зу-ль-" от "Зу-ль-Карнайн") - сознательно
  НЕ тронул. `?`/`!` тоже не тронул (несут смысл).
- `_clean_translation` (`core/mufradat.py`) чистит ТОЛЬКО при показе -
  честно объяснил пользователю разницу между "трогаем сам перевод
  слова" (не делаем) и "убираем связующий знак от соседнего слова"
  (делаем) - тот же принцип, что "*"/`_is_junk` из утренней части.
  Подключено в `get_words_in_range`/`get_words_by_progress_keys`
  (core/mufradat.py), `add_starred_word_by_progress_key`
  (core/mushaf_words.py, отложенный импорт - избегает цикла с
  core.mufradat), `get_ayah_words` (scripts/export_mushaf_page.py).
  Коммит `02fa2b8`.

### Критическая находка: реальный serving-путь

- При синке 604 JSON-файлов пользователь спросил "ты же вчера копировал
  604 файла?" - в процессе ответа обнаружил РЕАЛЬНУЮ проблему: nginx
  (`server_name yassir.ddns.net`) отдаёт файлы из `/var/www/yassir-app`,
  СОВСЕМ НЕ из `~/yassir-bot/mushaf_data`, куда я весь день делал
  scp/cp. `.github/workflows/deploy.yml` синкает туда ТОЛЬКО
  `index.html` при push - про `page*.json` (в `.gitignore`, GH Actions
  не видит их при checkout) ни строчки.
- **Последствие**: утечка "*" (утренний фикс ЭТОЙ ЖЕ сессии) была живой
  на реальном сайте ЕЩЁ ЧАСЫ после того, как я доложил пользователю
  "проверено, живое" - проверка была через SSH-чтение файла в
  `~/yassir-bot/mushaf_data`, не через настоящий сайт. Поймано только
  когда решил сверить через прямой `curl https://yassir.ddns.net/
  page2.json` вместо SSH-пути - увидел старые данные с запятыми, хотя
  файл на диске сервера был уже чист.
- **Фикс**: скопировал актуальные файлы в `/var/www/yassir-app`, затем
  заменил все 604 файла там на symlink на `~/yassir-bot/mushaf_data/
  page*.json` - теперь `cp`/`scp` в исходную директорию сразу
  отражается на сайте, разрыв закрыт навсегда (пока кто-то вручную не
  скопирует поверх symlink обычный файл). Все 604 страницы перепроверены
  через настоящий публичный URL параллельными запросами - 0 проблем.
- Записано в память Claude Code
  (`feedback_verify_via_public_url_not_ssh_path`) и в
  `wiki/mushaf_yassirapp.md` как жёсткое правило: контент `page*.json`
  впредь проверять ТОЛЬКО через публичный URL, никогда через SSH-путь.
  Коммит `592f97c`.

## Итог дня (окончательный)

- Все пять частей сессии закрыты, открытых задач нет. Финальные
  коммиты: `6b91d14`, `ad4d232`, `1411ceb`, `8b4c19c`, `02fa2b8`,
  `f5b5e53`, `592f97c`.
