# Сессионный лог — 04.07.2026

## Хадис про аш'аритов (проверка достоверности)
- Проверил хадис из тадаббур-поста про узнавание голосов аш'аритов и Хакима —
  реальный, Бухари/Муслим. Номер «Муслим №6250» изначально показался ошибкой
  (стандартная арабская нумерация даёт 2499), но выяснилось: это два разных
  стандарта нумерации одного и того же хадиса. По факту в `hadiths.db`
  `hadith_number` = внутренний `idInBook` из датасета `AhmedBaset/hadith-json`,
  не совпадает с научной нумерацией (Абд аль-Бакы) — пользователь подтвердил,
  что нужна именно она (2499)
- Решение: убрать номер хадиса из всех цитат насыха вообще (оставить только
  сборник), а не пытаться чинить маппинг номеров
- Из базы удалены 2 записи-дубля (Бухари id=4054, Муслим id=13527/№6250)
  по просьбе пользователя

## tadabbur_nasiha → Gemini
- Модель сменена с `deepseek/deepseek-v4-pro` на `google/gemini-2.5-pro`
  (проверено — модель реально есть на OpenRouter)

## Новая фича: проверка голосовых сдач устазом
- Студент шлёт голосовое (начитка/заучивание) → устаз реплаем (любым: текст/
  эмодзи/голосовой) или Telegram-реакцией отмечает проверенным
- Таблица `voice_submissions`, функции `save_voice_submission`/
  `mark_voice_reviewed`/`get_voice_review_stats` в `core/db.py`
- Ключевая ловушка (поймана до кода, через advisor): голосовой реплай устаза
  с разбором ошибки — тоже `voice`, разделено по `is_group_admin`
- `bot.py`: добавлен настоящий `reply_to_message_id` (раньше терялся) +
  заказан апдейт `message_reaction` у Telegram (не приходит по умолчанию)
- Утренний отчёт `voice_review_report()` в `core/scheduler.py`, шлётся в
  «Масштабирование» (-5283697370)
- Скорректировано по факту первого использования: было 07:00 за позавчера,
  стало **12:00 за вчера** — двое суток запаса оказались избыточны, устазы
  успевают проверить к обеду следующего дня
- Полная документация фичи: `wiki/voice_review.md`

## Женский бот
- Между делом обнаружено: `yassir-bot-female.service` падает каждые ~13с
  с 01.07 (нет `loginctl enable-linger` для `stursunkul`)
- Пользователь подтвердил: женский бот ещё не в реальной эксплуатации,
  чинить не нужно, приоритет — мужской бот

## Деплой
- Два пуша в master сегодня, оба через CI (`.github/workflows/deploy.yml`) —
  синтаксис + pytest + SSH-рестарт `yassir-bot.service` — прошли успешно
- Отправлен ручной срез статуса в «Масштабирование» (19:25) + позже сам
  автоматический формат скорректирован по фидбеку

## Вторая волна (вечер) — приглашение устазов в «Масштабирование»
- Пользователь попросил: раз в день (21:00) слать не-участникам «Масштабирования»
  из числа устазов короткое сообщение со ссылкой-приглашением
- **chat_id «Масштабирования» оказался битым**: группа апгрейднулась до супергруппы,
  `-5283697370` → `-1004387097146`. `getChatAdministrators`/`exportChatInviteLink`
  возвращали ошибку с точным новым id — Telegram сам подсказал. Бот сделан
  админом группы пользователем вручную, ссылка получена
- **Ошибся в диагностике устазов**: сначала проверил legacy-таблицу `group_admins`
  (4 строки, почти пусто) → решил, что надёжнее живой Telegram admin-статус →
  живой скан вернул 0 устазов. Пользователь напомнил про `/admin` реплаем —
  оказалось, реальные данные лежат в `user_groups.role='admin'` (не в
  `group_admins`), 13 устазов нашлись сразу. Подтвердил на живом кейсе: Руслан
  (G-4) закрыл отчёт Керима именно через эту таблицу
- Функция `invite_missing_ustaz_to_scaling()` переписана на правильный источник
  (`get_group_admins()`), задеплоена. Из 13 устазов — 3 уже в группе, 10 нет
- Первый автопрогон — 05.07 в 21:00 (сегодняшнее окно уже прошло к моменту
  деплоя — 21:17). Спросил пользователя, прогнать ли разово сейчас — ответа
  не получил, оставил на автоматику

## Вечерний ритуал
**Что закрыто:** фикс нумерации хадисов, смена модели ТДБР на Gemini,
фича проверки голосовых устазом (от обсуждения до деплоя, с доводкой
расписания по реальному фидбеку), фича приглашения устазов в «Масштабирование»
(с находкой и исправлением битого chat_id и неверной диагностики источника
устазов), найдена и задокументирована проблема женского бота (не чиним —
не приоритет)

**3 приоритета на завтра:**
1. Посмотреть первый реальный прогон `voice_review_report` — 05.07 в 12:00
2. Посмотреть первый автопрогон `invite_missing_ustaz_to_scaling` — 05.07 в 21:00,
   или разово прогнать раньше, если пользователь подтвердит
3. Продолжить открытые задачи из summary.md (привязка ID студентов, права
   бота-админа в группах, `migrate_to_chat_id` — теперь подтверждённая проблема)

**Лягушка первой:** посмотреть отчёт в 12:00 и убедиться, что формат читается
легко — если что-то не так, поправить раньше, чем накопится больше данных
