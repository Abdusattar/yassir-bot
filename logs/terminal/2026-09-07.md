# 07.09.2026

## Навигация по группам в кабинете устаза

Жалоба: до дальней группы приходится долго листать, «может каруселем?».

Посмотрел цифры: у супер-админа **17 групп**, полоса чипов показывает 3–4. Но
главное вскрылось не в свайпах: **числа долгов есть только у видимых чипов**,
то есть пока листаешь — не знаешь, где скопилось. А скопилось крайне
неравномерно: 334 неразобранных у «Отбор.» против нуля ждущих у «Группы для
детей».

Поэтому карусель отклонил (она лечит листание, а не слепоту) и предложил
список по тапу — принято. Свои группы сверху, дальше по числу ждущих. В
списке показаны **оба** числа: ждут за окно и сколько лежит старше; второе
раньше было не видно до захода внутрь.

Список абсолютный, а не в потоке — иначе раскрытие растягивало бы липкую
шапку. Проверено на 17 группах: шапка 94px, список скроллится внутри себя.

## Обрыв записи на 10 минутах

Жалоба «аудио длиннее 10 минут обрезаются» подтвердилась замером живых сдач:
пять записей ровно по 599.8–600.3 сек, Рауф налетел трижды.

**Просто поднять предел было нельзя.** Замер цепочки на сервере:
полная перекодировка 30 минут = 58.3 сек при таймауте ffmpeg 60, а у nginx
`proxy_read_timeout` не задан — дефолтные 60. Длинная сдача упёрлась бы в оба
и провалилась целиком — студент терял бы ВСЮ запись вместо обрезанной.

Чинил корень: Chrome пишет уже в opus, его достаточно переложить в ogg без
перекодирования — **0.4 сек вместо 58**. Safari даёт mp4/aac, там
перекодировка осталась, под неё подняты таймауты (ffmpeg 180, nginx 300,
конфиг с бэкапом, `nginx -t` пройден). Предел 30 минут; главное — обрыв стал
ВИДИМЫМ: за две минуты предупреждение, при упоре — прямое сообщение вместо
прежнего «всё хорошо, отправляй».

## CI поймал боевой баг с часовым поясом

Деплой упал, хотя правки касались записи и транскода. Причина не в коде, а во
времени запуска — после полуночи по Бишкеку.

`get_prep_students_active` считала `julianday('now','localtime')`, где
localtime — пояс МАШИНЫ (сервер и CI в UTC), а `joined_date` записан
бишкекскими датами. Расхождение 6 часов: **на проде дедлайн подготовительной
срабатывал позже срока**. Та же грабля, что была со `score_events`.

Передаём своё бишкекское «сейчас» параметром. Тест задаёт время жёстко —
покраснеет, если localtime вернут.

## Вердикты: два правила от пользователя

**Пересдача только с голосовым разбором.** Вернуть работу, не сказав почему,
значит оставить студента в тупике. Пометки слов остаются добровольными.
Проверка на сервере, а не только в кнопке. В кабинете — не отказ в последний
момент: кнопка ведёт записывать замечание.

**Гейт снимает САМА пересдача, а не её приём.** Довод пользователя:
«студентам важна стабильность сдач, ожидать и останавливаться это ломать
ритм». Непроверенных сдач больше полутора тысяч, разбирают неровно — студент,
честно перечитавший в тот же вечер, стоял бы днями не по своей вине.

Тонкость: засчитываем только сдачу, отправленную ПОСЛЕ вердикта. Устаз может
пометить на пересдачу старую сдачу, когда по тому же месту уже лежит более
новая — по одному id гейт снялся бы мгновенно.

## Экран «Сдачи» у студента

Данные, изменившие постановку: у активного студента 117–177 сдач, из них
**53–132 «ждут проверки»** — это очередь УСТАЗА, а не задача студента.
Показывать ему такой счётчик значит нагружать виной за чужой долг.

Поэтому не три равные вкладки, а асимметрия:
- «Нужно пересдать» — блок сверху и только когда не пустой (вкладка
  существовала бы всегда и заставляла заглядывать в пустоту);
- «на проверке N» — тихой строкой, без красного;
- зелёная «Пересдать» снята с каждой строки; у принятой её нет вовсе;
- **«Открыть это место»** — по вопросу пользователя: устаз мог отметить слова
  или наговорить разбор и при этом ПРИНЯТЬ работу. Раньше единственная дорога
  к подсветке шла через «Пересдать», которая включает режим заучивания — то
  есть посмотреть замечания при «принято» можно было только сделав вид, что
  идёшь пересдавать. Выбран широкий вариант: кнопка появляется и когда есть
  только голос (слушать разбор, глядя на строку, — не то же, что на слух).

## Прочее

- **Асель** — супер-админ женского бота. Добавлена В КОНЕЦ списка: первый
  используется для алертов, второй вычисляется как проверяющий учебной
  программы — вставка в середину сдвинула бы обоих. Проверено, что Умар им и
  остался. Конфиг с бэкапом.
- **Отчёт «Проверка ИИ»** поставлен на паузу (`VERIFY_REPORT_ENABLED = False`).
  Выключен именно ОТЧЁТ; сама проверка работ студентов в группах работает — её
  тихое отключение изменило бы то, что получают студенты. Лог пишется дальше.
- **Баннер «нет связи»** перестал кричать на каждый обрыв: успех гасит его
  сам, одиночный обрыв не показывается, в скрытой вкладке не считается. Но
  если приложение ни разу ничего не получило — сразу, без порога.
- **Перевод по тапу** больше не уезжает за край экрана. Вторая половина бага
  чуть не проехала: ширину нельзя мерить на прежней позиции — у
  `position: fixed` она считается «сколько влезет от left до края», и у самого
  края попап меряется узким. Поймано только замером координат.
- **Панель записи** стала нижней шторкой вместо модалки во весь экран: она
  перехватывала касания, и мусхаф под ней не скроллился, хотя умеет. Плюс
  кнопка «Пауза» (таймер считает без пауз).

## Открыто

1. **Женская подготовительная → супергруппа.** Действие пользователя в
   Telegram, сказал — завтра, ин ша Аллах. Бот к переезду готов: ловит
   `migrate_to_chat_id` и переносит группу вместе со студентами.
2. **Лилия** (женская, группа Асмы): спросить, что показал экран после
   переоткрытия — баннер «нет связи» означает канал, пустой экран без баннера
   означает копать дальше.
3. **Вердиктами почти не пользуются: три за всё время, пересдач ноль.** При
   121 сдаче через приложение половину разобрали реплаем в группе, по-старому.
   Блок «нужно пересдать» у студентов будет пустым при любой навигации, пока
   устазы не перейдут на разбор через кабинет. Экран к этому готов; разговор с
   Умаром и Асмой даст больше, чем правки интерфейса.

## Решения, закрытые пользователем

- «Бот переспрашивает, когда не понял сообщение» (история с «Поатор») —
  **остаётся как было**, не делаем.
- Навигация по группам — список, не карусель.
- Метки источника — «через апп» / «через группу» (его формулировка вместо
  моей «лист + аудио»).

## Уроки по четвергам: обе очереди стояли пустыми

Вопрос пользователя «если уроки закончились — подготовить к следующим
отправкам, как минимум 4» вскрыл больше, чем просто конец очереди.

Мужской нахв не уходил в группы **три четверга** (20.08, 27.08, 03.09),
таджвид — один (03.09). А женская база отстала сильнее всех: у неё очередь
СВОЯ (базы у ботов разные), стартовала позже, и 5 уже написанных уроков
таджвида — корень языка, середина языка, ад-дад, кончик языка, зубно-язычные —
в неё вообще никогда не заносились.

Причина тишины: предупреждение приходило ровно ОДИН раз, когда оставалась
последняя часть. Дальше пустой четверг был безмолвным no-op. Починено — теперь
пустая очередь сама шлёт напоминание, закреплено 4 тестами.

## Матны в репозитории

На вопрос «источники, книги же, они у нас в папках — оттуда берёшь?» честный
ответ был «нет»: в `knowledge/нахв/` лежит только ПЛАН устаза (оглавление), а в
`sources/` — материалы Тадаббура. Матнов не было, цитаты добывались поиском по
вебу каждый раз заново.

Скачаны с Wikisource в `sources/matn/` (оба в общественном достоянии):
Аджуррумия целиком и Джазарийя (107 байтов). `WebFetch` отдать матн целиком
по-прежнему отказывается — тянул через curl + API.

Сразу окупилось: **все 7 цитат новых уроков сверены по локальному тексту**
дословно, включая `فألف الجوف وأختاها وهي` и `والاسم الذي لا ينصرف يخفض بالفتحة`.
Заодно нашлась строка матна лучше той, что нашёл поиск.

## Подготовлено 8 частей (4 + 4)

- **Нахв**: `الجمع وأنواعه` целиком — ровно 4 части, как в плане устаза. Закрыт
  разбор исма по числу; дальше `فعل وعلامته`.
- **Таджвид**: джоуф и хайшум закрывают главу `مخارج الحروف` со сводкой 17 мест;
  открыта глава `صفات الحروف` — сифат и пять пар, затем `الهمس`/`الجهر`.

Хватает на 4 четверга вперёд (10.09 → 01.10), у женского таджвида — на 9
(до 05.11), потому что она догоняет мужскую.

Пополнение — через постоянный скрипт `/home/stursunkul/scripts/curriculum_parts.py`
(list / export / add), `add` идемпотентен по теме.

## Гейт пересдачи зависел от зернистости часов

При фиксации сессии пуш отвалился: `test_accepted_retake_unblocks` упал, хотя
двумя прогонами раньше был зелёным. Оказалось — плавающий тест, падает в 40%
запусков.

Причина настоящая, не тестовая. Гейт снимался условием `sent_at > verdict_at`,
а `datetime.now()` на Windows идёт шагом ~15 мс: две соседние записи получают
ОДИНАКОВУЮ метку, и строгое «больше» не срабатывает. То есть гейт снимался или
держался по воле часов. На Linux шаг микросекундный, поэтому CI и прод молчали
— но условие было неверным по сути.

Убрал время из решения совсем: в момент вердикта запоминаем id последней сдачи
этой единицы (`verdict_after_id`), а пересдачей считаем всё, что пришло после
неё. id монотонен и от часов не зависит. Тест с замороженным временем
покраснеет, если сравнение по времени вернут.

## Экран разбора: голос и лист вместе (вечер)

Жалоба по скриншоту: «попал в режим заучивания, красные слова есть, а где
аудио, чтобы слушать разбор и видеть пометки». Причина подтвердилась в коде:
разбор был разорван на два экрана — голос играл только в списке «Сдачи»,
пометки рисовались на листе, обе двери на лист аудио с собой не брали.

Макет согласован в три круга (артефакт 5581984e): сначала одна лента, потом
по замечанию пользователя — подсветка сданной единицы вместо бара со словами
(«полстраницы может не влезть — посмотрят скроллом»), потом ленты студента и
устаза порознь.

Сделано: `#look-foot` — полоса разбора в потоке колонки (не поверх листа,
поэтому отступ снизу не нужен), запись тянется один раз и держится blob'ом,
в пересдаче сворачивается в строку `#rec-review`. Вход один — «Разобрать»;
пересдача стала шагом внутри разбора, а не отдельной дверью к тем же пометкам.

**Отклонено пользователем**: привязка слов к секунде записи. Довод —
«студент видит слова, плюс аудио, устаз сам объяснит что и как». Согласен:
метки пришлось бы сбрасывать при каждой перезаписи замечания.

## Чужие кнопки: три бага, найденные вопросом «а зачем это устазу?»

Вопрос пользователя про ссылку «поправить» в кабинете устаза оказался не
косметикой. Шапка `#hifz-head` одна на все роли, поэтому:

1. Устаз в проверке мог тапнуть «поправить» и следующим тапом по чужой сдаче
   сохранить это место СЕБЕ (`hifzPickDone → hifzSave`).
2. Студент мог сменить место посреди пересдачи — запись уходила не туда и
   упиралась в `retake_pending`, а серверный указатель уже сдвинут.
3. Имя студента в шапке жило до первой перерисовки — `hifzApply → hifzHead`
   возвращал туда «стр. 15 · строка 3 из 8».
4. Свайп страниц в проверке: пометки адресуются (строка, слово) без номера
   страницы — на соседней ложатся на другие слова, и вердикт уходит с ними.

Всё закрыто: шапку рисует роль, свайп заперт при проверке и пересдаче.

## Проверка headless: две ловушки съёмки

Скриншоты сначала врали дважды. `fetch` по `file://` режется CORS — страница
мусхафа не грузилась вовсе (подняли локальный http). А `--window-size` на
Windows задаётся в физических пикселях: при DPI 125% окно 390 даёт вьюпорт
500 CSS px, и правый край экрана выглядел обрезанным — я чуть не пошёл чинить
целую вёрстку. Контрольный снимок обычного чтения показал ту же «обрезку» и
снял подозрение с новых экранов.

## Отмотка 10 секунд — и устазу тоже

Просьба пользователя по ходу реализации: дать «↺ 10 сек» не только студенту в
разборе, но и устазу, пока он слушает сдачу. Довод верный — устаз ловит ошибку
на слух и переслушивает место по нескольку раз.

Кнопка у устаза показывается только во время воспроизведения: до старта
отматывать нечего, а в ряду на 360px третьей кнопке иначе не хватает места
(проверено снимком — «⏸ Стоп», «↺ 10 сек», «🎙 Замечание» помещаются).

## Деплой

Пуш `24ca737`, Actions зелёный за 53 сек. Прод проверен **по публичному URL**,
не по SSH-пути: `https://yassir.ddns.net/index.html` отдаёт новую версию
(маркеры `look-foot` / `review-back10` / `rec-review` на месте, 200),
`Last-Modified` совпадает со временем деплоя, `Cache-Control: no-cache` —
телефоны перепроверят файл по ETag. Женскому рестарт не нужен: `index.html`
общий, Menu Button не менялся.

## Проверка в группе — и регрессия, которую она вскрыла

Вопрос пользователя: «те, кто сдаёт в группах, проверку получают там же —
надо считать это проверенным». Проверено по коду и по живой базе: это уже так,
`mark_voice_reviewed` ставит `reviewed_at` и на реплай, и на реакцию, а очередь
кабинета и зона «Проверено» смотрят именно на него. Цифры мужского бота: 5096
сдач, 3268 закрыты групповой проверкой, 1823 без реакции, 5 с вердиктом.

Но вопрос вскрыл мою же сегодняшнюю регрессию: убрав из списка кнопку
«▶ Разбор устаза», я оставил дорогу к разбору только сдачам с местом. А
разборов у сдач БЕЗ места — 839 против 30 у сдач из приложения. То есть
подавляющее большинство разборов стало недоступно студенту.

Починено: у сдач без места голос играет в самой строке списка (листа с
подсветкой у них не будет никогда), у сдач из приложения — на листе.

Отдельно видно, где проверка реально не идёт: «Отбор.» 25% и «Группа для
детей» 17% против 56–90% в остальных. Устазы там прописаны — вопрос к людям.

## Экран «Сдачи»: четыре правки пользователя

1. **«Перезаписать» — только до проверки.** У проверенной сдачи перезаписывать
   нечего.
2. **Хвост «ещё N старше» убран**, окно расширено с 3 дней до недели. История
   за месяц и так видна календарём выше.
3. **Сдачи голосом в группу больше ничего не проигрывают в приложении** — ни
   своей записи, ни ответа устаза. Отменяет мою правку часом раньше. Доводы
   пользователя: слушать надо там же, где сдавали (в группе), и — главное —
   бот пишет в сдачи ЛЮБОЕ голосовое студента в группе, поэтому «моя запись»
   там запросто окажется не сдачей. Плюс цель: сдавать через приложение должно
   быть выгоднее.
4. Групповая сдача в списке теперь просто отмечена: «проверено» и «Ответил N».

Проверено снимком на трёх случаях сразу: сдача из аппа на пересдачу (Моя
запись + Разобрать), групповая проверенная (кнопок нет), сдача из аппа в
ожидании (Моя запись + Перезаписать).

## Отложено: стимул сдавать через приложение

Предложил писать в строке групповой сдачи серой подсказкой, что через
приложение устаз видит саму строку и отмечает слова. **Решение пользователя:
пока не надо** — корректировать будем личными сообщениями, а не подсказкой в
интерфейсе.

## Картина направления — записана в wiki

Пользователь дал видение на ближайшее время, зафиксировано в
`wiki/app_direction.md`: плавный переход сдачи в приложение (группа живёт до
дедлайна), своё приложение вне Telegram на yassir.ddns.net (вход по номеру и
коду — код шлём в самого бота), тренажёры как общая механика закрепления
уроков (таджвид, нахв), раздел с тренажёрами вместо одной карточки.

Три технических факта, названные сразу, чтобы не завели в тупик позже:
initData вне Telegram не существует — нужен свой вход; `users.phone` это
Telegram ID, а не номер, настоящих номеров у нас нет; пуши на iOS у веба
куцые — напоминания остаются за ботом.

Порядок работ: функционал → дашборд → онбординг → тренажёры по урокам →
приложение вне Telegram.


## Дыхание: форма выбрана — сердце

Из двух вариантов (сердце / сетка) взяли сердце. Дальше по его правкам:

- **Волна суток — плавная и листаемая.** Не столбики, а кривая (Catmull-Rom),
  листается стрелками и свайпом на неделю назад. Предыдущий день остаётся
  бледной линией под текущим — тогда видно не срез, а саму волну.
- **Сегодняшняя волна дорисовывается до текущего часа**, дальше обрывается
  живой точкой: день ещё не случился.
- **Счётчик вместо «сколько людей».** Одно число действий за день, растёт на
  каждое событие, под ним разбивка (заучил / повторил / слова / разобрал).
- **Действия устаза идут в общий счёт** — волна единая, а не два графика.
- **Аудио-сдачу отдельной строкой не считаем:** голосовое в группе тут же
  становится заданием, двойной счёт дал бы два удара на одно действие.

Макет: артефакт «Дыхание Яссира».

### Какие действия есть в системе (7 дней, обе базы)

score_events category='task': заучивание 593, повторение 622, слова 609,
таджвид 228, нахв 137, хадис 16. Плюс разборы устаза 584 (voice_submissions.
reviewed_at), онлайн-урок 22 (attendance), узр 10. Служебные категории
(prep_offer, prep_juz_answer, prep_juz_q_nudge) — не действия джамаата.

Рост недели виден по дням: 30.08 — 257 действий, 06.09 — 518 (62 → 130 человек).

`scripts/pulse_stats.py` дополнен режимом `--by-day` — матрица день × час,
задания + разборы, для листаемой волны.

### Рекордная линия и тихие часы

- **Рекорд остаётся на графике пунктиром** — сейчас 6 сентября, 518 действий.
  Держится как планка; перебьёт другой день — рекордом становится он.
- **Ночью экран не пустеет.** Вчерашний день не исчезает в полночь, а остаётся
  тенью — силуэт сердца под новым днём, тает по мере того как разгорается
  сегодняшний. Проверено на `?h=4`: в четыре утра видно 16 действий, 5 человек
  и весь вчерашний контур.
- **Статики нет никогда:** удар сердца (44–90 уд/мин по часу) плюс медленный
  вдох-выдох между ударами. В тишине подпись меняется на «тихо · последнее
  действие N часов назад» вместо счёта.
- В макет добавлен параметр `?h=<час>` — посмотреть экран глазами того, кто
  зашёл в этот час.

Тень вчерашнего дня **догорает до фаджра**: до ~05:00 держится в полную силу,
дальше тает и к 10 утра исчезает. Заполнение сегодняшнего дня гасит её и само
по себе. Фаджр пока константа (5:00, Бишкек) — в реализации можно брать по
расписанию сезона.


## Дыхание реализовано в приложении

- `core/pulse.py` — ритм по часам и дням из ОБЕИХ баз (парная только на
  чтение, `mode=ro`), кэш 60 сек, `GET /api/muf/pulse`.
- Фронт: сердце (196 точек = активные студенты обеих баз), счётчик действий,
  волна с листалкой и рекордом. Вспышки — реальная дельта между ответами
  сервера, а не выдумка.
- `tests/test_pulse.py` — 6 тестов: общий счёт заданий и разборов, сложение
  двух баз, отсутствие парной базы, битая парная база, рекорд без сегодня,
  кэш. Всего в проекте 173 теста, все зелёные.

## Регрессия «не видно пометку устаза» — найдена и закрыта

Жалоба: сдача с вердиктом «на пересдачу» не видна вверху экрана «Сдачи».
Данные были целы (id 5074, verdict retake, 6 пометок, аудио-разбор Умар
устаза) — схлопывалась ВЁРСТКА: `#subs-body` это flex-столбец, `.subs-todo`
сжимался до красной линии, потому что `overflow: hidden` снял защиту
`min-height: auto`. Фикс: `#subs-body > * { flex: 0 0 auto; }`. Воспроизведено
на стенде с настоящим списком сдач — на коротком списке дефект не виден.

Задеплоено (коммит 0ac6290): мужской через Actions, женский — рестартом
`systemctl --user restart yassir-bot-female.service`. Оба отдают
`/api/muf/pulse` (401 без initData — роут жив). Прод на реальных данных:
198 человек в проекте, сегодня 177 действий от 61 человека, рекорд 6 сентября
— 529 действий.


## Новый стиль дверей дашборда

Плитки с эмодзи → список строк с фактами (стр. 15 половина листа / 12 из 40
слов / нужно пересдать 1 / ждут разбора 7). Знаки монохромные, из мусхафа.
Экран собран сверху вниз, а не по центру. Числа едут в heartbeat (facts),
новая `get_submission_counts()` в core/db.py. Тестов стало 175.

Задеплоено (1090387), оба бота. На живых данных двери показывают: указатель
стр. 15 этап 3 (страница целиком), «нужно пересдать 1», у устаза очередь.

Кнопки языка и темы переехали из абсолютного угла в строку заголовка: с
дыханием наверху они садились прямо на его карточку. Заодно чуть меньше
(36px) и порядок стал предсказуемым — dash-head с direction: ltr.


## Разгон дыхания при открытии

День собирается на глазах: 0 → сегодняшнее число, точки зажигаются, волна
прочерчивается, три подписи всплывают. Длительность от величины числа
(1300 + √N×95 мс, 1.5–3.2 с) — по замечанию пользователя: «цифры могут быть
большими, глаз должен уловить». Дата рекорда теперь на графике цифрами —
«рекорд 06.09 · 518», год не показываем.

Разгон замедлен: 1900 + √N×140 мс (2.2–4.8 с) и кривая smoothstep вместо
кубического ease-out — первая версия «мельтешила».

В блоке «нужно пересдать» кнопка стала «Пересдать» (акцентная, сразу к
записи) вместо «Разобрать»; разбор устаза подцепляется и при прямом входе.


## Серии голосовых склеены

Жалоба: «на проверке 8», из них четыре записи 02.09 10:23–10:31 — одна сдача,
разрезанная на куски. merge_submission_series() (окно 30 мин, только групповые
сдачи, ответ на часть закрывает серию). На данных пользователя 20 строк → 14,
ждут 8 → 3. Счётчик двери переведён на ту же функцию и недельное окно.

Заодно: подпись счётчика стала «N действий джамаата сегодня · к 18:00», само
число переливается (градиент по тексту), в двери «пересдать N».

Откат: кнопка снова ведёт в разбор устаза (не сразу в запись) — переименована
в «Разбор устаза». Ошибкой было слово, а не маршрут.
