# Сессия 05.08.2026

## Тема 1: обучающие видео для новичков подготовительной

- Собрал знание из кода/wiki (методика 40+40, задания m/r/t, «у» раз в
  неделю, вопрос про джуз при выпуске) + уточнил у пользователя то, чего в
  коде нет: адаб группы, что скриннить, точный формат сдачи каждого задания.
- Формат менялся дважды: сначала NotebookLM (1 длинный ролик 5-8 мин) →
  отклонено (не тот сервис, эпоха коротких видео) → реалистичная сцена под
  Gemini/Veo, 2 отдельных видео (сдача заданий / адаб и жизнь проекта).
- Первая попытка сгенерировать видео не удалась («видео не очень получилось»)
  — тема отложена пользователем, сценарии остались в `materials/` на будущее.

## Тема 2: аудит качества AI-проверки муфрадата — основная работа сессии

Пользователь несколько раз возвращал меня к более глубокой проверке
(«ошибок больше, копай дальше», «поголовно везде, а ты про проценты») —
каждый раз копал глубже и находил больше, но и сам себя поправлял, когда
собственные выводы оказывались неверными при проверке:

1. Начал с чтения кода/wiki → нашёл документированную историю фиксов
   (22.07, 27.07) в `wiki/ai_verification.md`.
2. Прямой доступ к серверу (`ssh claude-access@34.51.213.67`) — реальные
   данные `verify_log` (450+ записей), не гадание по коду.
3. Ошибочно предположил «словарь Корана неполный» → проверил напрямую →
   оказалось, это сознательная политика (алиф/хамза не считается ошибкой),
   а не баг. Признал вслух, не стал тихо продолжать по неверному пути.
4. Ошибочно чуть не назвал id 27 (22.07) живым багом → проверил дату
   фикса → оказалось, это ДО фикса от 27.07, уже решено. Прогнал реальную
   функцию на реальном тексте, чтобы подтвердить.
5. Настоящая корневая причина нашлась только на 4-й итерации: не индекс,
   не перевод отдельно — а общий паттерн «модель перечисляет верные пункты
   вместо молчания», согласно явному правилу промпта, которое Gemini 2.5
   Flash не соблюдает надёжно.

## Advisor review перед деплоем

Позвал advisor дважды (первый раз — перегружен). Ревью нашло 2 блокера:
- Точечные тесты не заменяют прогон по всем данным → сделал dry-run на
  всех 287 помеченных записях, не только на 5 придуманных случаях.
- Схлопывание «нет арабских токенов → тишина» было слишком широким, могло
  проглотить настоящую таджвид-правку целиком на кириллице → сузил до
  «схлопывать только голые заголовки разделов, и только если что-то реально
  вырезано и нигде нет признака ошибки».
- Информационно: регулярки только под русский → добавил «туура»
  (кыргызский), dry-run подтвердил 0 пропусков.
- Advisor отдельно предупредил не трогать женского бота заодно — женский
  деплоится вручную, там уже очередь с 02.08, рестарт ради одного фильтра
  выкатит весь неприменённый кластер разом.

## Результат

Коммит `abb07b0`, запушен, задеплоен на мужского бота автоматически
(GitHub Actions), подтверждено — процесс перезапустился в 12:58 с новым
кодом. Тесты зелёные (30/30). Женский бот не тронут — отдельная задача.

## Не сделано / открыто

- Мисхарактеризация реальных ошибок (id 448 — ошибка настоящая, но описана
  неверно, консонант ق↔ك назван огласовкой) — другая проблема, не сегодня.
- Женский бот всё ещё не получил ни сегодняшний фикс, ни весь кластер с
  02.08 (attendance-confirm, публичные ссылки, prep-редирект).
- Видео для новичков подготовительной — сценарии готовы, генерация
  отложена пользователем.

## Методологический урок сессии

Пользователь был прав, настаивая на более глубокой проверке несколько раз
подряд («мне кажется что ошибок больше») — каждый раз находилось больше,
чем в предыдущем проходе. Но не всегда интуиция указывала на РЕАЛЬНУЮ
причину с первого раза (гипотеза про словарь оказалась красной селёдкой) —
здесь помогло не поверить собственному первому объяснению и перепроверить
его тем же способом (прогон реальной функции на реальных данных), прежде
чем предлагать код-фикс. Стоит записать в память Claude Code отдельно.
