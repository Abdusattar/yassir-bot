# Лог сессии 2026-06-24

## Исправления (продолжение 23.06)

### Бакыт/Эмиль — устазы отвечали AI как студентам
- Фикс: `if is_group_admin → return` (безусловный)
- Потом скорректировано: суперадмин-студент (Абдусаттар в G2b) должен сдавать отчёты
- Итог: `if is_group_admin and not s: return` — проверяем active student role

### chat_member update — бот молчал при вступлении по ссылке
- Добавлена обработка `chat_member` update в bot.py
- Причина молчания: `sqlite3.Row.get()` → AttributeError при каждом вступлении
- Фикс: `group_info["group_type"]` вместо `group_info.get("group_type")`

### Имя через ИИ — extract_name
- Заменили regex-зачистку на `ai.extract_name(text)`
- Мухаммадали и Байыш переименованы вручную в БД

### Онлайн урок
- Теперь: устаз пишет `/lesson` → урок открыт → студенты пишут "+" → `/endlesson` → итог
- Без открытого урока "+" тихо игнорируется

### /admin и /unadmin — только реплай
- Убраны все варианты (@username, имя, телефон)
- help-текст обновлён: `/admin — (реплаем) назначить устаза группы`

### /setgroup — авто-назначение устазов
- После регистрации группы вызывает getChatAdministrators
- Все Telegram-админы (кроме суперадминов и ботов) → устазы группы автоматически

### Один студент — одна учебная группа
- get_learning_group(phone) — проверка перед добавлением

### SUPER_ADMIN_IDS
- Переименовано с ADMIN_IDS/ADMIN_PHONES → SUPER_ADMIN_IDS везде
- Умар (7666229019) добавлен как суперадмин

### added_date — timezone bug
- SQLite date('now') возвращает UTC → Bishkek UTC+6 → после 18:00 новый студент писался вчерашней датой
- Фикс: передаём get_date() явно при INSERT users

---

## Сессия 24.06.2026 (вторая половина дня)

### Утренний отчёт в тадаббур не отправился
- Причина: chat_id тадаббура изменился при апгрейде до супергруппы (-5106301574 → -1004428339471)
- Фикс: UPDATE в БД + добавлена обработка migrate_to_chat_id в tg_call

### tg_call молча глотал ошибки
- Фикс: логируем когда ok:false

### Двойной бот (409 конфликт) — системный vs user-level
- Причина: start.sh запускал второй экземпляр поверх systemd
- start.sh заменён заглушкой
- Создан manage.sh
- User-level сервис переименован в .service.disabled (не активен)
- Деплой: `scp + sudo /bin/systemctl restart yassir-bot`
- Два процесса bot.py — НОРМА (мужской male + женский female)

### Перенос фич из старого бота (все перенесены)
- ✅ Утренний отчёт в тадаббур (07:00)
- ✅ Кик за 10 пропусков/месяц (про, было 14)
- ✅ Кик за 3 пропуска онлайн уроков/месяц (про, было 4) + перевод в тадаббур
- ✅ Кик незарегистрированных через 14 дней
- ✅ Бонус +5 за каждые 7 дней стрика
- ✅ AI-похвала при 7/14/30 стрике
- ✅ 3 дня отсутствия → absent_motivation (18:00)
- ✅ Вечерний отчёт не шлём в тадаббур (только внутри групп)

### Порог переводов
- pro: 14 → 10 пропусков/месяц
- relaxed: 30 → 20 дней подряд без отчёта

### Баг "НЕТ ·" — регистрация с неверным именем
- Причина: AI возвращал "НЕТ ·" вместо "НЕТ", проверка exact-match не срабатывала
- Фикс: extract_name проверяет первое слово (strip точек/тире), не всю строку
- Fallback: если extract_name(сообщение) → None → пробуем extract_name(sender_name)
- Авто-матч: при первом сообщении ищем незарегистрированного с точным именем из Telegram (только если один такой)
- Заур (N-2a) и Умар (G-5) переименованы/слиты вручную в БД

### Устаз-чекмарк bug (Умар, G 2b)
- Умар (суперадмин) реплаил "✓" на аудио студента → бот спрашивал "напиши словами что сдал"
- Причина 1: у Умара была активная student-роль в G 2b
- Причина 2: `if is_group_admin and not s: return` — не выходил если student есть
- Фикс 1: деактивирована student-роль Умара в G 2b
- Фикс 2: `if is_group_admin(phone, group_id): return` — безусловно

## Договорённости
- sqlite3.Row не поддерживает .get() — всегда использовать []
- Не делать ничего руками в БД без явного запроса — находим причину и фиксируем в коде
- Деплой: только sudo /bin/systemctl restart yassir-bot, никогда --user
- Два процесса bot.py на сервере = норма (male + female)
- Правило: сначала обсуждение и подтверждение — потом код

---

## Сессия 24.06.2026 (вечер — продолжение)

### Кыргызский язык (i18n)
- Добавлены полные переводы ru/ky/uz/kk/en в i18n.py: справка студента, справка устаза, prep-строки
- `task_name(key, lang)` — название задания на языке группы
- `help_student(group_tasks, gtype, lang)` / `help_admin(lang)` — функции справки
- G-8 (lang=ky) теперь получает все сообщения на кыргызском

### Промпты AI переведены на английский
- Причина: AI путался при русском промпте + кыргызский вывод → присылал только "Салам"
- Все промпты в ai.py на английском
- `lang_instruction()` тоже на английском
- AI корректно переключается на нужный язык группы

### Ключевые слова для кыргызского и муфродат
- TASK_WORDS["t"] добавлены: "муфродат", "муфродаты" (через "о", не "а")
- TASK_WORDS["t"] для кыргызского: "создор", "унутулган", "жаны создор", "жаңы сөздөр", "котормолор"

### Подготовительная группа (prep)
- Новый тип группы: /settype prep
- core/prep.py — полная реализация: check_prep_students (21:00), send_prep_reminders (07:00), handle_prep_callback
- 14 дней, ≥5 отчётов → inline кнопки выбора языка → ссылка на наименьшую relaxed-группу
- <5 отчётов → деактивация, остаётся в Тадаббуре
- /setlink https://t.me/+xxx — устаз сохраняет invite_link
- groups.invite_link (TEXT) — добавлен в БД через ALTER TABLE в init()

### Ссылки групп сохранены в БД
- N-1: https://t.me/+NulMkvo53CdhMDVi
- N-2a: https://t.me/+D5lPx0IY47FiYWZi
- Yassir Тадаббур: https://t.me/+8dP2yljXPtJmM2Ey
- Yassir подготовительная: https://t.me/+TRIIDKBi6bAwMjc6
- G-5, G-10, G-8, G-2c, G-6, G2b — сохранены ранее

### Дубль G-6 (id=12) деактивирован
- id=12 — пустой (только Айдин без Мурада)
- id=13 — правильный (оба студента), оставлен

### AI-проверка — исправления и расширение
- Добавлена проверка хадиса (h) — раньше не было
- AI отвечает реплаем на исходное сообщение студента (reply_to_message_id)
- message_id прокинут через: bot.py → queued_process_message → process_message → _verify_and_reply → send_message
- send_message(chat_id, text, reply_to_message_id=None) — новый параметр
- Верно → ✅ (реплай), ошибка → 🤖 Ясир: (реплай)
- Промпт: на английском, справочник PROGRAM_INFO в конце

### Wiki создана
- wiki/i18n.md — многоязычность
- wiki/prep_group.md — подготовительная группа
- wiki/ai_verification.md — AI-проверка отчётов

### /help@botname — фикс команд с суффиксом
- Telegram в группах добавляет @botname к командам при клике из меню
- Стрипаем @botname только для студенческих команд: /help, /mystats, /rating, /id
- Админские команды (/settasks и др.) с @botname — игнорируются намеренно

### Устаз в личке → AI-помощник
- Если пишет в личку устаз (is_any_group_admin) → ai.answer_ustaz_question()
- Если не знает → "уточните у супер-админов (Абдусаттар или Умар)"
- is_any_group_admin(phone) добавлена в db.py

### Валидация имени при регистрации
- ai.is_valid_name(name) → YES/NO — проверяет что извлечённое слово реальное имя
- Если сомнительно → "Вас так и зовут — «X»? Напишите своё настоящее имя 🙂"
- ask_name_confirm добавлен в i18n.py (ru/ky/uz/kk/ar/en)

### Устаз-студент bug (add_group_admin)
- При назначении /admin student-роль в той же группе деактивируется
- Айдин (G-6) исправлен вручную

### AI-проверка → реплай на сообщение студента
- message_id прокинут: bot.py → queued_process_message → process_message → _verify_and_reply → send_message
- send_message(chat_id, text, reply_to_message_id=None) — новый параметр в tg.py

### Чистка БД
- Удалены: группа "Умар Умарахунов" (id=6), дубликат G-6 (id=12), фантом "НЕТ" (id=40)
- Убраны · и пробелы в именах: Залкар, Абдурахман, Иззат, Нурлан, Акын, Мурад, Бактияр, Илиас
- Мустафа (G-5) переименован из "Толук"
- Нурсултан (Yassir подготовительная) — устаз группы
- G-4 и G-11 — добавлены invite_link

### Архитектура знаний
- wiki/ и context/ — только для Клода (память между сессиями)
- Телеграм-бот читает ТОЛЬКО core/content.py (PROJECT_INFO + PROGRAM_INFO)
- Чтобы бот "знал" что-то новое → нужно добавить в content.py

---

## Сессия 24.06.2026 (третья часть — оптимизация токенов)

### Фото + caption считается как отчёт
- `is_media=True` → `check_text(text, media=True)` — без арабского фильтра для "t"
- Убрано условие `len(text.strip()) > 3` для медиа-сообщений

### Детекция отрицания
- `_is_negated(t, idx)` в db.py — ищет слова отрицания в том же клаузе перед ключевым словом
- Граница клауза — последний знак препинания перед idx
- "я не буду сдавать переводы" → задание "t" НЕ засчитывается

### Разбивка PROGRAM_INFO на секции (оптимизация токенов)
- content.py: `_PROG_TAJWEED`, `_PROG_NAHW`, `_PROG_MUFRADAT`, `_PROG_METHOD`
- `PROGRAM_INFO = все 4 вместе` (для полного контекста)
- `PROG_SECTIONS = {"j": tajweed, "n": nahw, "t": mufradat, "h": mufradat}`
- `_build_reference(checks)` — для AI-проверки отчётов, берёт только нужные секции
- Экономия 60-80% токенов при проверке отчётов

### Оптимизация answer_question
- `_build_reference_for_question(text)` — keyword-детекция темы вопроса
- Если тема уверенно определена (ровно 1 секция) → только та секция
- Если неясно или несколько тем → весь PROGRAM_INFO (safe fallback)
- Всегда включает `get_knowledge()` (правила устаза)
- Эдвайзер подтвердил: для вопросов fallback обязателен, иначе риск галлюцинаций

### Задание: AI-проверка в relaxed группах (G-5, G-8 и др.)
- В группах без Arabic text "t" не триггерит AI-проверку (нет арабики в тексте)
- media=True частично решает для фото, но text-only без арабики не проверяется
- Ещё не решено

### Бот молчал при вступлении по ссылке
- Причина 1: is_tadabbur блокировал всех входящих в Тадаббур → откатили (Тадаббур не трогаем)
- Причина 2: prep группа мигрировала в супергруппу (-5461393490 → -1004331909785), старый chat_id в БД
- Фикс: UPDATE groups SET chat_id='-1004331909785' WHERE id=14
- Причина 3: 8 групп где бот не admin → chat_member апдейты не приходят (G2b, G-4, G-9, G-12, Отбор., Дети, G-7)
- Добавлено логирование в chat_member и new_chat_members блоки bot.py
- Открыто: migrate_to_chat_id не обрабатывается автоматически → нужно добавить в bot.py
- Открыто: сделать бота админом во всех группах
