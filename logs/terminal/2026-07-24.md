# 2026-07-24

## Качество AI-проверки (нахв/таджвид/муфрадат)

- Разобраны 85 записей `verify_log` вручную, построены 3 детерминированных
  пост-фильтра поверх LLM-ответа (муфрадат/хадис-фантомы, таджвид-молчание,
  нахв-точные-фантомы). Принцип: фильтром чинить только байт-в-байт
  сравнимые случаи, не грамматическое суждение (предупреждение эдвайзера,
  подтверждено дальше в сессии).
- 3 спорных грамматических вопроса отправлены Умар устазу в группу
  «Yassir масштабирование», получены ответы (идафа→معرфة, لام в لَكُم — харф
  джарр, "нет роли — нет иъраба" подтверждено). Внесены точечные правки в
  `knowledge/prog_nahw.md` и промпт (`core/handlers.py`), проверены на живой
  модели (не только на историческом датасете — правка промпта
  недетерминирована, скрипт-фильтр её не проверяет).

## Подготовительная группа — тайминг-баг и полный пивот

- Найден реальный баг: детекция условия (07:00) и уведомление устазу (было
  только 21:00) — разные процессы; поздний вечерний отчёт (после 21:00)
  ловился только на следующий вечер. Добавлена дублирующая утренняя
  проверка.
- **Решение Умар устаза**: бот сам распределяет выпускников подготовительной
  без участия человека, кроме тех, кто знает >1 джуза или хафиз (для них —
  отдельная группа N-1). Механизм: 2 кнопки в личке ("знаешь ли 1 джуз?") →
  автоматическая ссылка на нужную группу (N-1 для "да", наименее заполненная
  relaxed для "нет"). Устаз подключается только если что-то пошло не так
  (нет группы со ссылкой) — тогда личный алерт пользователю через 24ч
  застоя на любом шаге.
- Первая callback_query/inline-keyboard механика в проекте — протестирована
  живьём на личном chat_id перед доверием (кнопки пришли, пропали после
  нажатия — подтверждено).
- Эдвайзер поймал блокер до деплоя: гейт "спросили" коммитился до реальной
  отправки → если личка недоступна, вопрос терялся навсегда. Исправлено на
  "повторять, пока не ответит/не вступит" + fallback в саму группу, если
  личка ещё не открыта студентом.
- Мурад — первый живой студент на новой автоматике, вопрос отправлен и
  подтверждён (`ok: true` от Telegram API). Устазу сказано игнорировать
  старое ручное сообщение по нему (осталось от прогона до пивота).
- **Первый живой прогон подтверждён**: Мурад ответил, перешёл в G-6, кик
  из подготовительной сработал сам. Найден и исправлен попутный баг —
  объявления о переходе показывали "его(её)"/"брата/сестру" вместо чистого
  мужского рода (тексты писались вручную вместо использования уже
  существующего `_apply_gender`). Убран заодно мёртвый ключ
  `prep_graduate_notify_admin`.

## Договорённости

- Личный алерт про "застрял 24ч+" — пользователю (`SUPER_ADMIN_IDS[0]`), не
  Умару.
- "Группа для детей"/"Отбор." сейчас исключены из авто-подбора неявно (нет
  invite_link) — отмечено как хрупкое место на будущее.
