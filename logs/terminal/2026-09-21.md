# 21.09.2026

## «Перезаписать» тащил прежние попытки в отправку

Жалоба пользователя: сдавал третий этап, отправил — первые 4–5 секунд
прозвучали трижды. Подтвердил сам: «Перезаписать» нажимал несколько раз.

- Причина — вчерашняя фича кусков (576a3c5): «Перезаписать» не стирал
  IndexedDB, все попытки с seg=0 и тем же местом склеивались в один Blob.
- Воспроизведено: ffmpeg на склейке трёх live-webm — 6+6+9 с → 20,9 с;
  живое приложение (`scripts/check_rec_redo.py --old`): 3 потока, 20,9 с;
  после фикса: 1 поток, 9,0 с.
- ffprobe склейку НЕ видит (длина = последняя попытка). Признаки: звук при
  раскодировании длиннее ffprobe; число заголовков потока > 1.
- Фикс (план согласован с пользователем): стирание по «Перезаписать»,
  очистка перед записью с нуля, куски из хранилища только при >1 сегменте,
  журнал сервера «склеена из N потоков».
- Попутно найдено и исправлено: `/hifz/submit` брал только `audio`,
  продолжение сдачи 40+40 после обрыва терялось.
- Поиск пострадавших: `scripts/find_glued_recordings.py` (след не помогает —
  байт/сек у склейки пользователя обычные, 3608).

## Прочее
- Первый этап (строчка) кусками не хранится — решение пользователя, `df545a6`.
- Старую пригласительную мужской подготовительной пользователь сбросил 20.09 — пункт закрыт.
- Решение пользователя: розыск пострадавших за короткое окно бага не нужен («ушло ушло»).

## Хусари без щелчков — пересобрано и на проде
- «Кыйч» — не стыки аятов (там паузы), а края файла: рез по границе слова
  посреди слитного чтения, обрыв до 10 996/32 767, слышен на каждом повторе.
- `build_unit`: один проход ffmpeg, atrim + afade 12 мс на каждом отрезке,
  одно кодирование 128k. Строке переход отдельным отрезком — иначе
  захватывается пауза Muallim для ученика (пилот поймал +9 с тишины).
- Пилот стр. 5 одобрен на слух. Пересобрано 105 стр. / 1874 файла, 0 ошибок,
  длины ±0,34 с. Выборка 40 строк: конец медиана 2801 → 496, громких 12 → 0.
- На сервер частями по 20 стр., md5 всех 1874 совпали, публичный URL сверен.
  В приложении `?v=2` (HUSARY_AUDIO_VER) — nginx не шлёт Cache-Control.

## Часовой пояс студента — шаг 1: только сбор
- Приложение шлёт IANA-пояс телефона в heartbeat, сервер пишет `users.tz`
  при смене (проверка по pytz). NULL = Бишкек (подтвердил пользователь).
- Отчёт: `scripts/user_timezones.py --profile male|female`.
- Решение пользователя по шагу 2: пропуски подводить, «когда день закончится
  во всех поясах» — одна отложенная проверка на всех, а не своя на каждого.
- Шаг 2 по поясам — НА ПАУЗЕ (решение пользователя). Выбран вариант (б):
  проверка пропусков одна, когда день кончился у самого западного студента.
  Отдельно понадобится (а) — дата сдачи по поясу студента. Сбор `users.tz` идёт.

## Чтение лёжа (просьба братьев) — первый шаг: чтение + запись повторения
- `#app.land`: телефон лёжа (landscape, высота ≤ 500) И лист чтения И не
  заучивание. Кегль до 64 (стоя 34), колонка во всю ширину, меню скрыто,
  строка страниц в конце листа, панель записи повторения — строка 54px.
- Решения пользователя: буквы на всю ширину; сначала повторение, заучивание
  (этапы 2–3) — потом; «проверь, чтобы другие вещи никуда не съехали».
- Проверка `scripts/shoot_landscape.py` (Chrome+WebKit, 9 экранов × 5 размеров,
  попиксельно против HEAD) — проблем нет. По пути пойманы и исправлены:
  пропажа строки страниц на «Смысле», сдвиг шапки «Моих слов», порядок шапки
  заучивания, место чтения в WebKit (0,50→0,33), лист после «›» открывался
  в конце (было и стоя на длинных листах), сжатый плеер на SE.
- Лёжа верх листа срезало «потолком» (justify-content: center в колонке) —
  скрин пользователя; исправлено + прокрутка по строкам (`cdf589a`).
- Урок: мелкую правку сначала выложить и показать, долгую проверку — фоном
  параллельно, а не ждать её (пользователь: «долго!!!»).

## Итог дня
Закрыто: склейка попыток записи, Хусари без щелчков, сбор поясов, чтение лёжа
(чтение + запись повторения), манифест, старая ссылка prep (сделал пользователь).
