# 08.09.2026

## Пересдача перекрывается продолжением сдачи (этапы 2/3)

Решение пользователя (вариант 1): сдача той же единицы, пришедшая после
вердикта, снимает долг — карточка уходит из блока «ПЕРЕСДАТЬ» в общую ленту
с нейтральной пометкой «пересдал — ждёт устаза», счётчик на двери гаснет.

Механика гейта это уже умела с 07.09 (`verdict_after_id`), не знали о ней
только «долговые» экраны студента. Сделано:
- `_REDONE_EXISTS` в `core/db.py` — одно правило на все три места;
- `get_student_submissions` отдаёт `redone`;
- `get_submission_counts` не считает отвеченные пересдачи;
- фронт: `todo`/`feed`, пилюля, выделение кнопки разбора.

Побочно: у сдач ИЗ ГРУППЫ (hifz_page пуст) «та же единица» неопределима,
поэтому долг там гасит любая следующая групповая запись. Это существующее
поведение гейта, распространили на счётчик — осознанно.

## Счётчик 40+40 терял введённые цифры

Жалоба: «сдавал этап 3 два вечера, писал цифрами, а вчера показало 0/80».

Проверено по проду, не по догадке: у Сатара (272581710) в
`mushaf_hifz_progress` последняя запись — 05.09 (стр.15, этап 2). За 06 и
07.09 записей нет, хотя сдачи есть (5074 retake, 5168). В журнале за
07.09 23:56 виден GET прогресса по стр.15/этап 3 — панель открывалась, —
а POST не ушёл ни разу ни в один из двух вечеров.

Причина: число из поля «своё» подтверждала только маленькая кнопка «ок».
Большая нижняя «Сохранить и продолжить» в поле не заглядывала — просто
закрывала панель. Кнопка обманывала своим названием.

Фикс: нижняя кнопка сперва досохраняет введённое (ждёт ответ сервера —
от него зависит, дошло ли до 80 и двигать ли указатель), потом закрывает.

## Дальше

Онбординг-раздел на дашборде (своя иконка): как сдавать повторение, как
муфрадат через тренажёр, как заучивание. Делаем по одному, через макеты.

## Онбординг в приложении (начало)

Название двери: **«Работа с устазом»** (было «Заучивания»). Причина: сдают
заучивание в Мусхафе, а за этой дверью — что я отправил и что ответил устаз;
старое имя обещало кнопку сдачи, которой там нет. Переименован и `look_back`
(«‹ Заучивания» → «‹ К моим сдачам»). Пятая дверь онбординга — **«Как учимся ✎»**,
подпись «Повторение, муфрадат, заучивание».

Структура онбординга: Заучивание (как сдать, Мусхаф) · Работа с устазом
(ждёт → разбор → пересдача) · Повторение · Слова. Разбор — отдельный блок:
это другое место и другой ритм, и смотреть его стоит даже когда приняли.

### Как снимаются экраны

`scratchpad/mockup/devserver.py` — локальная копия приложения: своя пустая
БД, свой hadiths.db, авторизация подменена, index.html настоящий (две правки:
заглушка Telegram.WebApp и apiBase без профиля). Сцены `?scene=` **кликают по
настоящим кнопкам** — переименуют кнопку, съёмка упадёт, а не соврёт.
Телефонная ширина — через iframe (`/frame`), headless Chrome сам её не держит.
`build_onboarding.py` собирает страницу, картинки внутрь как data: URI.

Живой аккаунт для съёмки не годится: чтобы снять шаги до конца, надо реально
отправить запись — она ушла бы устазу в группу.

### Заметки пользователя на блок «Слова» (не начат)

- забытые слова пополнять двойным тапом в мусхафе — по ним отработка чаще;
- не бояться ошибаться: слово с ошибкой само уходит в «Мои слова» и выходит
  чаще (`record_answer`, `add_starred_word_by_progress_key`);
- выходит из списка после MASTERY_STREAK = **3** верных подряд (не 5, как
  сказано в комментарии рядом с кодом);
- 40 слов за день (`DAILY_WORDS_FOR_TASK_CREDIT`) закрывают задание автоматом
  + сообщение в группу;
- тап по слову = дословный перевод, двойной = «Мои слова» (окно 300 мс).

## Кабинет устаза: свои группы по умолчанию (08.09)

Жалоба «голова кружится в устаз» оказалась не про права устазов: 19 обычных
устазов и так видели только свои группы. Все 18 активных валились ровно
двоим супер-админам (пользователь и Умар устаз, `SUPER_ADMIN_IDS`), у Умара
своих четыре. Решение пользователя — вариант «свои + кнопка»:
кабинет открывается на своих, чужие за строкой «показать остальные (14) ·
ждут 23». Без своих групп (случай пользователя) чужие показываются сразу.

Важно не спутать: **видимость сузили, права нет.** `_allowed_submission`
считает разрешение со `scope="all"` — подхват чужой сдачи, ради которого
«все группы» и заводились 04.09, работает как работал.

## Правило: поменяли экран — перевыпустить онбординг

Договорённость пользователя 08.09: после переделки места в приложении через
день-два обновлять онбординг по этому месту. Технически это одна команда —
`python scripts/shoot_onboarding.py` пересобирает ВСЕ снимки, кликая по
настоящим кнопкам; тексты шагов лежат рядом с ними в `LEARN`
(mushaf_data/index.html).

## Единый принцип навигации — согласован, не реализован

- верх слева «← Откуда» (возврат по стеку, с названием места);
- верх справа ✕ (закрыть режим или шторку поверх экрана);
- низ — только действия (Сдать, Записать, Дальше, Закончить).

Что не по правилу сейчас: «Крупно» (кнопка «На страницу» внизу), 🔙 в
таббаре мусхафа для «Смысл»/«Мои слова», «⬅️ Назад» в рейтинге тренажёра,
«← Студенты» в карточке студента, ✕ режима заучивания стоит слева.

## Онбординг устаза: раздел «Проверка сдач» (08.09)

Пятый раздел «Как учимся», виден **только устазам** — не «заперт», а
отсутствует: у студента этих экранов нет. Признак берём из того же
heartbeat, что зажигает дверь «Устаз» (`meIsUstaz`), и если ответ пришёл,
пока человек стоит в оглавлении, — перерисовываем.

Восемь шагов: дверь → «Ждут» (вкладки групп, «через апп»/«через группу») →
открытая сдача и лист студента → тап по слову = пометка ошибки → 🎙
замечание (для пересдачи обязательно) → ✅ / ↺ → «Проверено» («ждёт
пересдачи» vs «пересдал») → «Студенты».

Стенд съёмки научился второму аккаунту: `USTAZ = 888002` — устаз двух групп
(N-1, N-2a), сцены `u_*` снимаются под ним (`/frame?as=ustaz`). Появился
`--only` — перевыпустить часть снимков, не гоняя все 29.

## Найдено на съёмке: кабинет показывал все группы разом

`ustazRenderList` фильтровала сдачи по `ustazGroupFilter` ДО того, как та
выбиралась по данным. На самом первом рендере фильтр был `null` — в списке
лежали сдачи всех групп, хотя вкладка сверху уже называла одну. Живёт с
4ac6805 (04.09), то есть у Умара устаза его четыре группы всё это время
показывались вперемешку до первого тапа по вкладке. Фикс: сначала выбрать
группу, потом фильтровать.

## Дверь онбординга переименована (08.09)

«Как учимся» → **«Как работаем»**: в разделе теперь и глава устаза, а
«учимся» про неё не говорит. Тронуты четыре видимые строки (дверь, заголовок
экрана, возврат к оглавлению «← Как работаем») — снимки перевыпускать не
пришлось: дверь в кадр дашборда не попадает, она пятая сверху.

---

## Итог дня (вечерний ритуал)

**Закрыто сегодня:**

1. Пересдача больше не висит долгом, если студент уже начитал это место
   заново (вариант 1: карточка уходит в ленту с пометкой «пересдал — ждёт
   устаза»); общее правило `_REDONE_EXISTS` в `core/db.py`, два теста.
2. Найдена и закрыта настоящая причина «0/80»: повторы не сохранялись, потому
   что «Сохранить и продолжить» не дожидалось ответа сервера. Пострадавшим
   отправлено извинение в личку.
3. Раздел онбординга «Как работаем»: четыре главы для студента + пятая
   «Проверка сдач» только для устазов, всё на снимках самого приложения.
   Конвейер съёмки: два аккаунта, `--only`, снимки стареют вместе с UI.
4. Кабинет устаза открывается на своих группах (чужие — по кнопке), при этом
   права на подхват чужой сдачи сохранены.
5. Попутно: дверь «Заучивание» → «Работа с устазом»; 🔖 отодвинута от стрелок;
   свайп по шагам онбординга; фикс «первый рендер показывал все группы».

**Три приоритета на завтра:**

1. Единый принцип навигации — начать с мусхафа: «Крупно» («На страницу» вниз
   → верхний возврат) и 🔙 в таббаре на «← Мусхаф»/«← Тренажёр». После правки
   сразу перевыпустить снимки `big`, `bookmark`, `mywords`.
2. Показать Умару устазу главу «Проверка сдач» и собрать замечания — он
   единственный, кто пользуется кабинетом каждый день.
3. Долги из списка: счётчик дней до перевода (висит с 19.07) либо Абдуллах
   N-2a.

**Лягушка первой:** навигация в мусхафе. Она трогает экраны, которыми
пользуются каждый день, и требует свежей головы — а после неё перевыпуск
снимков идёт уже механически.

## Вечер: ИИ кончились кредиты, Имран, банк насых (08.09)

**Кик Имрана.** Он ведёт «Отбор.» и учится в N-1 — устазы у нас сами студенты
(правило пользователя). Кик за пропуски, prep, подтверждение Умара и ссылка в
N-1 — всё это отработало правильно; сломался только вход: `is_any_group_admin`
(04.09) глушила его для любого, кто где-то устаз. Регистрация не прошла,
выпуск не оформился, метка «вернуться через prep» осталась — и наутро первое
же сообщение выкидывало его из группы. Четыре круга.
Разделяет теперь `joins_as_student` (core/db.py): метка возврата / активен в
prep / уже был студентом в этой самой группе → учёба; иначе → пришёл вести.
Имран дооформлен `scripts/finish_prep_graduation.py`, ему отправлено извинение
со ссылкой.

**Цикл «Напиши только своё имя».** У OpenRouter кончились кредиты (402 с 07.09
23:41), `extract_name` возвращала None и при «не имя», и при «ИИ молчит» —
регистрация встала: Салия написала имя 9 раз, потом «Салия. М.» ещё 8.
Теперь простое имя распознаётся локально (`looks_like_plain_name`), а
`ai.AI_DOWN` отделяет молчание ИИ от отказа. Заодно минус два ИИ-вызова на
каждую регистрацию.

**Расходы на ИИ (замер по ключу).** День $0.012, неделя $0.37, сентябрь $1.70,
всего $37.39 из купленных $50. Боты стоят копейки; кредиты съели разовые
батчи (классификация Корана на claude-haiku). Что растёт с джамаатом —
персональные насыхи: 40 вызовов в сутки только на «скучаем, вернись».

**Банк насых.** Решение пользователя: пока просто копить всё, что бот сочиняет
(личное, групповое, Тадаббур, похвалы), выдачу делать через месяц.
`core/nasiha_bank.py` — общий sources/hadiths.db, имя → `{name}`, повтор
считается в `seen_count`. Проверено живым вызовом на проде.

## Тон писем бота (08.09, замечание пользователя)

Бот пишет **от первого лица**: «я не пустил», «я исправил» — не «мы починили»
и не «прости нас». Ошибку признаёт спокойно и один раз, без посыпания пеплом:
он не вредил намеренно. Замечание пришло на письмо-извинение Имрану, но
правило общее — для всех текстов бота.

## Итог вечера

Закрыто: цикл «Напиши только своё имя» (и минус два ИИ-вызова на регистрацию),
кик Имрана и правило «устаз тоже студент», банк насых начал копиться, замер
расходов на ИИ с выводами.

Открыто на завтра:
1. Салия и Зульфия — попросить написать имя ещё раз (висят в pending_names).
2. Предупреждение админу, когда ИИ молчит подряд: сегодняшний сбой сутки был
   виден только в журнале.
3. Единый принцип навигации — мусхаф («Крупно», 🔙 в таббаре), затем
   перевыпуск снимков онбординга по этим местам.

Лягушка: навигация в мусхафе — трогает экраны, которыми пользуются каждый день.
