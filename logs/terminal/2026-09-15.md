# 2026-09-15 — сессия (тренажёр слов: второй порог зачёта)

## Решения

- **Зачёт «Слова» = 40 разных слов И 20 из них верно** (решение пользователя).
  «Верно» — хотя бы раз за день, позднейшая ошибка метку не снимает.
  Константа `DAILY_CORRECT_FOR_TASK_CREDIT = 20` рядом с `DAILY_WORDS_FOR_TASK_CREDIT`.
  Единый источник истины — `get_daily_words_status` в core/mufradat.py
  (count / correct / done); API, пилюля, дашборд пороги сами не сравнивают.
- **Чатовый Telegram-путь тренажёра убран целиком** (пользователь: «он больше
  не нужен»), вместе с `/muftop` (подтверждено отдельно: «убрать»). Удалены
  callback-ветки muf:/mufinc/mufdec/mufend/muftop/muflang* в bot.py, текстовый
  ввод страницы в handlers.py, карточки/меню языка/рейтинг-текст в
  core/mufradat_bot.py, core/mufradat_render.py + шрифт assets/fonts/, из
  requirements — uharfbuzz и freetype-py. mufradat_bot.py теперь — серверная
  часть сдач из приложения (зачёты, хифз, вердикты, дивизионы).
- **«Верно за сеанс» на карточке → «✅ верно сегодня 14/20»** (просьба
  пользователя). session_correct из API и состояния убран.
- **Верное слово не повторяется в тот же день.** Причина повторов (вопрос
  пользователя «зачем правильные за день выводить по несколько раз»): корзина
  закрепления (04.09) брала слова со стриком 1–2, куда сегодняшнее верное слово
  попадало сразу. Фикс: `exclude_keys` в generate_question/pick_question_word из
  `get_today_correct_keys`; действует и на «Мои слова»; снимается, если иначе
  пул пуст. Слова с ошибкой сегодня остаются в обороте.

## Показ (без лишнего шума, но понятно)

- Пилюля в шапке: «32/40 сегодня · ✅ 14/20», зелёная только по `daily_done`.
- Подсказка после ответа: «Ещё N слов и M верных…»; когда 40 есть, а верных
  нет — «40 слов есть ✅ Для зачёта «Слова» ещё M верных».
- Дверь «Тренажёры»/дашборд: «✅ Y/20 верных» дописывается только когда 40
  набрано, а зачёта нет.
- Поздравление: «40 слов проработано, 20+ из них верно».
- Локализация ky/uz — позже одним проходом (T() откатывается на русский).

## Миграция / выкладка

- `mufradat_daily_answered_words` получает колонку `correct` (ADD COLUMN, при
  первом обращении). Строки до выкладки — correct=0: кто уже начал день,
  верные наберёт из оставшихся слов. Лучше выкладывать не посреди дня.

## Проверено

- 339 тестов зелёные (`python -m pytest -q`), новые — tests/test_mufradat_daily.py
  (7 штук: пороги, липкость метки, миграция, исключение и его снятие).
- Инлайн-JS index.html проходит `node --check`.

## Попутно (стенд)

- Стенд shoot_onboarding писал прогресс тренажёра в НАСТОЯЩУЮ sources/hadiths.db:
  core/mufradat.py импортирует HADITHS_DB по имени, подмена sampler.HADITHS_DB
  его не задевала. Добавлена явная подмена `_mufradat.HADITHS_DB`.
- Сцены trainer_* на стенде шли мимо хаба «Тренажёры» (с 14.09 вторая кнопка
  trh-words) и снимали хаб вместо карточки — переведены через хаб. Стенд поймал
  реальный баг: в handle_state остался старый вызов `_new_question(user_id, 0, None)`
  → 500 на /state. Исправлено до выкладки.
- Шрифт ScheherazadeNew-Regular.ttf на проде лежит копией в /var/www/yassir-app
  (обычный каталог, 27.08), не берётся из repo — assets/fonts/ оставлен как есть.
- Снимки онбординга сцен trainer_* устарели (хаб + новая пилюля) — перевыпустить
  через день-два (правило feedback_reissue_onboarding_after_ui_change).
- Выкладка: df1bcf9 запушен по слову пользователя (~16:00 Бишкек, локальный
  `TZ=Asia/Bishkek date` на Windows врал), Actions зелёный, прод сверен по
  содержимому, женский перезапущен руками. Правило перехода: кто сдал — сдал,
  остальные по новому.
- Поправка к своему же «наблюдать за "слова закончились"»: невозможно, исключение
  верных снимается при пустом списке кандидатов, None даёт только пустой основной
  пул. На маленькой закладке крайний случай — повторы верных к концу дня, это
  задуманный откат.
- Онбординг: перевыпуск снимков. Стенд копил дневной счёт слов и старые сдачи в
  TMP-базе (сцены устаза показывали пустую очередь) — set_state теперь ставит
  12/7 для сцен тренажёра и ноль остальным, TMP-база стенда пересоздана.
  Подписи trainer_q/answer/daily обновлены под два порога.
- Локализация ky/uz новых строк — пользователь отложил («пока не сегодня»).
- Полный проход по онбордингу (вопрос пользователя «все блоки перепроверил?» — нет,
  сначала смотрел только 5 из 29). Найдено: сцена progress была сломана ещё до
  сегодня (в репо лежал экран «С какой строчки начинаем?» вместо «Сколько сделал
  сегодня?») — стена пересдачи (13.09) не пускала на этап 2, плюс приложение
  читает указатель один раз на старте, и state() из сцены на него не влиял.
  Стенд: PRE_STATE в frame — состояние ставится ДО загрузки страницы, каждая
  сцена с чистого листа; для stage2 пересдача закрывается повторной записью
  (message_id 199). Остальные 24 снимка и подписи сверены — совпадают.

## Утреннее «переходи в приложение» (06:00)

- Задача пользователя: тем, кто сдаёт через группы, каждый день в 06:00 в личку —
  переходи в приложение, как зайти, в «Знаниях» показано как сдают, спроси
  одногруппников. Уточнение: устазы-студенты тоже получают; исключены только
  супер-админы, служебная группа и те, кому нельзя в личку.
- Критерий без новой схемы: зачёт задания за 3 дня есть, следа в app_trail нет.
  Откроет приложение — письма прекратятся сами.
- Код: `app_switch_recipients`/`app_switch_reminder` в scheduler.py (слот 06:00),
  `users_seen_since` в app_trail.py, `student_ids_with_tasks_since` в db.py, текст
  `app_switch_reminder` в i18n.py (ru), тест tests/test_app_switch_reminder.py.
- Ловушка Windows: heredoc в Bash-инструменте съедает обратные слэши — строка с
  `\n` получила настоящие переносы; чинить через chr(92).
- Вопрос пользователя про OpenRouter: все генерируемые тексты бота (core/ai.py)
  идут через OpenRouter `_or_call`: по умолчанию deepseek-v4-flash, кыргызский —
  Gemini Flash, насыхи — deepseek-v4-pro. Утреннее письмо про приложение — статичный
  текст, без ИИ.
