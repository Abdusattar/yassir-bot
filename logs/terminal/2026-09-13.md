# 13.09.2026

## Гейт пересдачи: правило стоит не там (разбор + решение)

**Как было до сегодня.** Гейт в `core/mufradat_bot.py:392` отбивал сдачу
ЛЮБОЙ единицы, кроме самого долга — включая соседнюю строку того же этапа
(подтверждено тестом `tests/test_verdict.py:182`: долг стр. 6 строка 7,
сдача стр. 6 строка 8 того же этапа 1 → `retake_pending`). Узнавал студент об
этом только ПОСЛЕ записи: в интерфейсе предварительной проверки нет, кнопка
«Сдать» открывает шторку без условий (`index.html:6579`), отказ приходит
ответом сервера (`index.html:6556`). Пользователь поймал это на себе 12.09.

**Решение (13.09).** Правило: **долг не мешает доделать этап, в котором
стоишь; долг закрывает вход в следующий.** «Внутри первого этапа 7 или 8
строк» — это ровно строки половины листа (`hifzHalfRange`).

Согласовано:

1. **Стена — на смене этапа, не на отправке.** Внутри этапа 1 строки идут
   свободно. Стена встаёт на 1→2, 2→3, 3→следующая страница.
2. **Блок только ДО действия.** Если ход закрыт, кнопка «Сдать» не открывает
   запись вообще, а становится дверью в пересдачу. Записывать впустую
   студент не должен ни разу.
3. **Стеной считается любой незакрытый долг**, а не только самый свежий.
   Сейчас `get_blocking_retake` (`core/db.py:1745`) берёт одну запись —
   последний вердикт `retake`, — и, пересдав её, студент проходит мимо более
   старого непересданного долга. Дыру закрываем.
4. **Долг виден заранее** — компактная пилюля в пустой середине шапки
   заучивания (`#hifz-head` это `space-between`: ✕ слева, место справа).
   Вторую строку в шапку НЕ возвращаем: её свели в одну 11.09 по просьбе
   пользователя (`index.html:367`), она отъедала место у мусхафа.
5. **После пересдачи — возврат на своё место, включая лист.**

**Подтверждённый баг (чтение кода, 13.09).** `hifzRetakeFinish`
(`index.html:7669`) восстанавливает `line`/`stage`/`on`, но `loadPage` не
зовёт — проверены все 20 вызовов `loadPage` в файле, на обратном пути его
нет. `currentPage` остаётся на странице долга, а отправка берёт страницу
именно оттуда (`index.html:6512`). После пересдачи на чужой странице шапка
показывает чужое место, и следующая сдача уйдёт с чужой страницей. Лечится
сейчас только выходом ✕ и повторным входом (`hifzEnter`, строка 5956).

**Порядок работы:** пользователь просил сперва макет, потом код.

## Очередь «потом» (поднято 13.09, не трогаем сейчас)

- **Навигатора по страницам нет в режиме заучивания.** Зайдя в 40+40,
  перескочить на другую страницу неудобно: `#topbar` и `#page-nav` спрятаны
  (`index.html:348`), остаются свайп, колесо и клавиши, а прямого перехода по
  номеру (`#page-indicator`) не видно.
- **Санжар (2б), 12.09: ушли 3 записи, которых он не собирался отправлять.**
  Подозрение пользователя — либо кнопка «стоп» отправляет сама, либо дело в
  плохой записи. Проверить путь `hifzRecStop` → `hifzRecSend`.
- **Прогресс-бар «Мой день» при 5–6 заданиях.** У групп с расширенным
  набором (таджвид, нахв) цифры, похоже, не помещаются. Снять по факту на
  группе с 5–6 заданиями, а не на стенде с тремя.
- **Эмулятор iPhone для мусхафа.** На айфонах шрифт листа скачет при
  перелистывании (то мельче, то крупнее), и на крупном текст вылезает за
  горизонтальные края. Нужен способ увидеть это отсюда, не держа айфон в
  руках. Смотреть `clampFontToFit`.

## Макет стены пересдачи (13.09)

`scripts/shoot_retake_gate.py` — тот же приём, что у `shoot_dash_variants`:
поверх НАСТОЯЩЕГО экрана заучивания накладывается только предлагаемая
разметка. Снимки в `logs/retake_gate/`, стенд на порту 8802
(`--serve`, `/vframe?v=<вариант>`). Варианты: `now`, `now_block`, `free`,
`wall_a`, `wall_b`, `wall_c`.

## Сделано 13.09

**Сервер**
- `core/db.py`: `get_open_retakes()` — ВСЕ незакрытые пересдачи, порядок по
  месту в мусхафе (страница, строка, этап), только долги с местом (голосовая
  сдача в группу места не несёт, пересдать её из приложения нечем —
  запереть человека такой нельзя). `get_blocking_retake()` стал обёрткой
  «первый из открытых». Добавлен `has_submission_for_unit()`.
- `core/mufradat_api.py`: `_hifz_retakes()` отдаёт приложению список долгов и
  признак `done` («на своём месте работать нечего»: этап 1 — строка сдана,
  этапы 2/3 — счётчик 40+40 дошёл до 80, половина считается по настоящему
  числу строк листа). `GET /hifz` отдаёт это вместе с указателем.
  **Стена переехала в `handle_hifz_set`**: смена этапа или страницы при
  открытом долге отбивается (409 `retake_pending`), первая постановка места
  не блокируется никогда.
- `core/mufradat_bot.py`: гейт на отправке остался только страховкой от
  устаревшей страницы — принимаем то, что стоит на указателе (та же страница
  и этап), либо сам долг.

**Приложение (`mushaf_data/index.html`)**
- Пилюля `● пересдать` в пустой середине шапки заучивания (`#hifz-debt`),
  видна пока ход ОТКРЫТ; тап ведёт в «Работу с устазом».
- `hifzNext` считает шаг ДО движения: внутри этапа строки идут свободно,
  смена этапа при долге уводит в «Работу с устазом».
- Вход в заучивание при закрытом ходе ведёт туда же, минуя лист.
- `openSubs` гасит `#app` — раньше сюда приходили только с дашборда, и
  мусхаф оставался поверх (поймано на стенде).
- `hifzSave` понимает отказ сервера: указатель не двигается, человек идёт к
  долгам.
- **Починен баг с листом**: `hifzRetakeSaved` запоминает страницу, и
  `hifzRetakeFinish` возвращает лист на своё место.
- Блок «ПЕРЕСДАТЬ» сортируется по месту в мусхафе; подсказка сокращена до
  «Пересдай — путь открыт.» (пользователь резал её четыре раза).

**Проверка**
- `tests/test_verdict.py`: старый тест «сдать другую единицу нельзя» заменён
  на новый — внутри этапа МОЖНО, прыжок на другой этап нельзя, долг всегда
  можно. Добавлен тест на порядок долгов и на то, что старый долг держит
  после пересдачи свежего. Весь набор — 299 тестов, зелено.
- Стенд: `scripts/shoot_retake_gate.py --only real_free,real_wall` снимает
  живое приложение в обоих состояниях.
- Тест писал указатель в настоящую `sources/hadiths.db` (забыл фикстуру
  `test_hadiths_db`) — поправлено, строка-мусор `777001` удалена.

## Насыха обращалась не тем полом (13.09, живой случай)

Имран получил личную насыху со словом «Сестра»; женщины иногда получают
«брат». Причина: `_GENDER_ADDR` (`core/ai.py`) зашивается в системный промпт
**при запуске процесса** из `_IS_FEMALE`, а личная насыха за несданный день
кэшируется по ключу `дата|miss:<язык>` в ОБЩЕЙ `hadiths.db` — без профиля
бота (`core/scheduler.py:_miss_nasiha_text`). Какой бот проснулся первым, тот
и сочинил; второй берёт текст дословно.

Решение пользователя — **вариант 2: общий текст бесполый** (не разводить кэш
по ботам ради лишнего вызова ИИ каждое утро). Добавлены `_NEUTRAL_ADDR` и
`_MOTIVATIONAL_SYSTEM_NEUTRAL`; `morning_miss_nasiha` идёт по ней, плюс
прямой запрет в задании. Персональные тексты (не общие) пол сохранили.
Тест-сторож в `tests/test_miss_nasiha_cooldown.py`.

Тадаббур-насыха тоже общая, но там бесполость уже требовалась промптом
(`ai.py`, ветка `neutral`) — её не рвало.

## Права женского бота (проверено 13.09)

`bot_rights.py --db female`: админ в 12 группах из 13 (было 3 из 13 на
09.09). Остаётся **«Yassir подготовительная»** — там статус `left`, бота в
группе нет вовсе. Женская подготовительная работает мимо бота.

## Панель записи: юзабилити (13.09)

Разбор по просьбе пользователя. Последовательность «Сдать → Записать → Стоп →
Отправить» признана верной: «Сдать» это намерение, «Записать» — готовность
читать вслух. Резать шаг не стали — шторка, открывающаяся уже пишущей
(`flow_go` на стенде), отправляла бы устазу первые секунды возни.

Сделано:
- **Мёртвый `0:00` убран** из простоя: таймер появляется вместе с записью.
  Шторка ниже, листа видно больше.
- **«Стоп» перестал быть зелёным** — своя красная рамка (`.stop`). Зелёная
  главная кнопка значит «хорошее итоговое действие», остановка им не
  является, а стояла на месте «Отправить» и того же цвета.
- **Защита 600 мс** на только что перерисованный ряд кнопок — палец, летящий
  вторым тапом по «Стоп», больше не попадает в «Отправить». Это и есть
  лечение случая Санжара (2б, 12.09: ушли 3 записи, которых не собирался
  отправлять).
- **Порог длины** `HIFZ_REC_MIN_MS = 2000`: обрывок короче двух секунд не
  уходит, вместо этого «Слишком коротко — прочитай ещё раз».

Не сделано, осталось в очереди: свой плеер вместо системного
`<audio controls>` в состоянии «готово» — сейчас там серая браузерная полоса,
разная на каждом телефоне, при том что в разборе устаза свой плеер уже есть.

Проверено на стенде живым кодом: `flow_ask`, `rec_live`, `rec_done`.
Весь набор тестов — 300, зелено.

## Страница в заучивании: ловушка закрыта, навигация вернулась в «поправить» (13.09)

**Ловушка.** Свайп в режиме заучивания не был заблокирован: `pageTurnBlocked`
сторожил только проверку устаза и пересдачу. Лист уезжал, а место — нет.
`hifzApply` подсвечивал ту же строку уже на НОВОЙ странице, шапка писала
новый номер, и сдача уходила с чужой страницей (она берёт `currentPage`,
см. `hifzRecSend`). На экране всё выглядело нормально — потому и не всплывало.
Достижимо одним пальцем.

Теперь `pageTurnBlocked` возвращает true и при `hifz.on && !hifz.picking` —
свайп, колесо и клавиши в заучивании не листают. Побочно закрылся и разбор
устаза (`look`): он тоже привязан к одной странице (пометки хранятся парой
строка+слово, без номера листа), а в списке блокировок его не было.

**Навигация в «поправить».** `#page-nav` был спрятан во всём режиме
заучивания, включая выбор места, — и чтобы поставить место на странице в
тридцати листах отсюда, надо было тридцать раз свайпнуть. Добавлено
исключение `#app.hifz.hifz-pick #page-nav`: в режиме выбора полоса
`🔖 « ‹ стр. N › »` на месте, тап по номеру открывает ввод страницы.

Проверено на стенде: `?v=pick_nav`.

## «Мой день» при длинных наборах заданий (13.09)

Замер по проду: длинные наборы всего у трёх групп из тридцати — у мужчин одна
на шесть заданий (`m,r,t,j,n,h`) и одна на пять, у женщин одна на пять.
Остальные — обычные `m,r,t`.

Ломались не цифры сами по себе, а счёт целиком: **«0 из 6» переносилось** на
«0 из» / «6», от этого росла вся полоса, а подписи под полосками сжимались до
слипания («Заучивание» вплотную к «Повторению»).

Сделано (вариант A, выбор пользователя):
- `white-space: nowrap` на счёте в шапке;
- класс `.many` на `#my-day` при пяти и более заданиях, подписи в нём 9px с
  отрицательным трекингом. Подписи НЕ убираем: по ним человек понимает, что
  именно не сдано (вариант B с их удалением отклонён).

Снято на стенде живым кодом: `?v=day3`, `?v=day5`, `?v=day6`.

## Свой плеер в панели записи (13.09)

В состоянии «готово» стоял системный `<audio controls>` — серая полоса, вид у
которой свой на каждом телефоне и ни на что в приложении не похожий. При этом
свой плеер в приложении давно есть: в разборе устаза (`#look-play` и соседи).

Теперь в панели записи тот же: круглая зелёная кнопка, дорожка с заливкой,
текущее время и длительность, перемотка тапом по полосе. Стили НЕ копировались
— расширены существующие селекторы (`#look-play, #hifz-rec-play` и т.д.), то
есть плеер один на оба места. Тег `<audio>` остался мотором, наружу не торчит.

Тонкость: длительность только что записанного blob браузер часто отдаёт как
`Infinity` (webm без длины в заголовке) — в таком случае берём собственный
замер `rec.ms`, он и так уже посчитан для порога минимальной длины.

## Шрифт мусхафа на айфонах: автоувеличение iOS (13.09)

Жалоба: на айфонах шрифт листа то мельче, то крупнее при листании, а на
крупном текст вылезает за горизонтальные края.

**Замер вместо догадок.** Написан постоянный скрипт
`scripts/measure_mushaf_fit.py`: поднимает стенд, проходит страницы и печатает
подобранный кегль и факт вылезания. Умеет headless Chrome и Playwright
(`--engine webkit --device "iPhone 13"`), а также нарочно тормозить файлы
шрифта (`--slow-font`).

Результаты:
- Chrome, 39 страниц, ширины 320 и 390 — **ноль вылезаний**; кегль 17.7–22.4
  и 21.8–27.6. Почти весь разброс даёт стр. 2 (начало Аль-Бакары, короткие
  строки); обычные листы отличаются на ~1 px.
- WebKit с профилем iPhone 13 — **то же самое**, ноль вылезаний.
- WebKit с задержкой шрифта 1500 мс — **то же самое**: защита
  `document.fonts.ready` перед `clampFontToFit` отрабатывает, гонки нет.
- Пользователь поправил: скачки он видит на ОБЫЧНЫХ листах, не на началах
  сур. То есть машинные замеры и живая картина расходятся — значит дело в
  движке iOS, а не в формуле.

**Причина.** В файле не было `-webkit-text-size-adjust`. iOS Safari по
умолчанию раздувает текст сам, оценивая ширину блока относительно экрана, и
коэффициент считает для каждой страницы свой. Наш `clampFontToFit` подбирает
кегль и строит justify под один размер, а Safari рисует другим — отсюда и
скачки, и вылезание. Функция только для iOS: ни Chrome, ни настольный WebKit
её не имеют, поэтому замеры с машины и показывали чистые нули.

**Сделано:** `-webkit-text-size-adjust: 100%` на `html, body` (100%, а не
none — пользовательский зум остаётся). Плюс потолок самопроверки поднят с 20
шагов до 40: двадцати хватало на промах в 18%, дальше цикл выходил, оставив
текст за краем.

**Не проверено на живом айфоне** — с машины это невозможно в принципе.
Подтверждать после выкладки, на телефоне.

## Пересдача из разбора сбивала место (день)

**Случай пользователя:** стоял на стр. 16, строке 3, пересдал строку 1 — место
в приложении осталось на строке 1.

**Причина.** `lookOpen` ставит `hifz.line/stage` и лист на место долга (своё
место уходит в `look.saved`). `lookRetake` обнулял `look.saved`, а
`subsRetake(item, true)` запоминал «место до пересдачи» уже из `hifz.line`,
то есть место долга. После отправки `hifzRetakeFinish` честно возвращал туда.
Пересдача прямо из списка «ПЕРЕСДАТЬ» (без разбора) не страдала.

**Прод:** указатель в `sources/hadiths.db` цел (16 / idx 2 / этап 1), сдача
5613 ушла, база не тронута. Опасность была бы дальше: сдача строки с этого
экрана через `hifzNext` → `hifzSave` откатила бы серверный указатель назад.

**Фикс:** `look.saved` хранит и страницу; при `fromLook` пересдача берёт место
из `look.saved`; обнуление перенесено после вызова. node --check чисто,
test_hifz_pointer 24 passed.

## Анализ кабинета устаза (день)

Прод мужского бота, сдачи из приложения с 30.08: 273, проверено 228, из них
вердиктом в кабинете 134, только реакцией/реплаем в группе ~94. В кабинете
работают Умар, Муслим, Эмиль, Тахир; в G-6, G-2c, G-5, G-4, G-7 вердиктов 0.
Медиана ожидания 11,7 ч (нед. 37), p90 25,7 ч.
Очередь забита голосовыми из группы, которые в кабинете не открыть: N-1 —
9 из приложения против 76 сырых записей из группы.

Найдено в коде: после вердикта openUstaz сбрасывает фильтр группы → список
может прыгнуть в другую группу; «Далее» нет. В проверке нет полосы/длительности
записи. Нет истории попыток по той же единице (одна строка ушла на пересдачу
5 раз). Выбор группы в «Ждут» и в «Студенты/Проверено» — две разные переменные.
Карточка студента — тупик (без его сдач). Реализация не начата, ждёт выбора.

## Выложено: место после пересдачи из разбора

Коммит 9f97046, пуш в master, 300 тестов в хуке прошли. Публичная страница
yassirilm.com отдала новый код через ~10 сек. Женский бот делит статику —
рестарт не нужен (правка только в index.html).

## Макеты кабинета устаза

`scripts/shoot_ustaz_cabinet.py` (стенд 8803, своя база yassir_ustaz_stand,
чтобы не засорять стенд онбординга). Снимки и сравнения в logs/ustaz_cabinet/:
очередь (А — голосовые одной строкой, Б — отдельным срезом), полоса записи,
«замечание есть — ждём вердикт», «после вердикта — дальше», прошлая попытка
рядом, календарь студента + его сдачи.

Проверено по коду: `save_submission_review` не ставит reviewed_at — замечание
без вердикта оставляет сдачу в «Ждут». Реализация не начата, ждёт выбора.

## Перепроверка анализа кабинета (пункты 2–6)

Пользователь: звук в порядке, устазы проверяют, жалоб нет — цель «ещё удобнее».

- Женский бот: из 187 сдач из приложения вердикт в кабинете у 2, остальное
  реплаем в группе; медиана ожидания 2,4 ч (мужской 12,7 ч).
- Перескок группы ПОДТВЕРЖДЁН на стенде (scratchpad/probe_ustaz.py): выбрал
  N-2a → открыл Юсуфа → «← Устаз» → снова N-1. Выбор N-2a в «Ждут» не
  переходит в «Проверено»/«Студенты» (там N-1), и возврат в «Ждут» тоже
  сбрасывает на N-1.
- Дверь «Устаз» и сводка в личку считают ВСЕ непроверенные за 3 дня, включая
  голосовые из группы: на стенде дверь 13, открыть можно 5.
- Серии в очереди устаза НЕ склеиваются (merge_submission_series только у
  студента) — каждый кусок отдельной строкой.
- «Замечание без вердикта» на проде: 0 случаев на обоих ботах — макет
  comment_then снимаем с приоритета.
- Поправка к анализу: «76 против 9» было с 01.09, окно кабинета — 3 дня.

## Исправлено: группа в кабинете устаза больше не сбрасывается

Один выбор на весь кабинет (ustazGroupId вместо ustazGroupFilter +
stuGroupId). Сброс только входом с дашборда — там группа, где ждут больше
всего (решение 04.09 сохранено). Выход из проверки — на ту вкладку, откуда
открыли. Чужие группы супер-админа не сворачиваются после проверки.

Проба на стенде (scratchpad/probe_ustaz.py), все 11 шагов сошлись:
N-2a → проверка → назад = N-2a; «Проверено»/«Студенты» = N-2a; из
«Проверено» проверка → назад = «Проверено»; вход с дашборда = N-1.
Не проверено: супер-админ со свёрнутыми чужими группами (на стенде его нет).

Следующим по списку: «Дальше» после вердикта (пользователь спросил, что это —
объяснено, решения ещё нет).

## «Дальше» после вердикта (сделано, до выкладки)

После «Принято»/«На пересдачу» в проверке из «Ждут» — ряд «К списку» /
«Дальше: Имя · стр. N ›», в подсказке «в группе ждут ещё N». Следующая —
та, что стояла ниже в «Ждут» той же группы (иначе первая оставшаяся);
список берётся заново с сервера. Голосовые из группы пропускаются. Нечего
больше — как раньше, назад в список. Из «Проверено» «Дальше» нет.

Ловушка учтена: reviewOpen запоминает место устаза только при ПЕРВОМ входе
в проверку — иначе «Дальше» вернул бы устазу строку прошлого студента.

Проба на свежей базе (scratchpad/probe_next.py): Абдулла стр.3 стр-ка 3 →
Абдулла стр-ка 4 → Хамза → Ибрахим → закрыто, осталось 0 открываемых;
своё место устаза стр. 7, строчка 5 — не тронуто.
Ряд «Дальше» стоит первым в #review-foot: снимки онбординга подсвечивают
вердикт как .row:last-child.

## Выложено: «Дальше» (ab4f98b) и честное число на двери

«Дальше» на проде, сервер на ab4f98b.

Решение пользователя: на двери и в сводке — только сдачи из приложения;
голосовые из группы в «Ждут» — вариант А (одна строка внизу).
- app_only в _pending_reviews_sql и обёртках; дверь, группы, хвост, сводка —
  с app_only=True; окно «Ждут» отдаёт всё, фронт сворачивает голосовые.
- Тап по строке раскрывает голосовые на месте (ссылки на группу в Telegram
  в приложении нет — openTelegramLink не используется нигде).
- Сводка: «ждут проверки в приложении: N». Женским устазам с одними
  голосовыми сводка теперь не приходит — по решению.
- Хвост «старше 3 дней» тоже только из приложения.
Стенд: дверь 5 вместо 13, N-1 «ждут 4», строка «Голосовые в группе · 8».
Тесты 302. Серверный код — женский бот нужно перезапустить руками.

## Выложено: честное число на двери (842b3f3) + скрипт макетов (6375379)

Сервер pull 842b3f3 в 06:47:23 UTC, мужской бот рестарт 06:47:24
(system.slice), женский перезапущен руками 06:47:49 (user.slice), оба active.
Скрипт макетов закоммичен, стенд 8803 остановлен.

Осталось из разбора кабинета (удобство, не починка): полоса записи в
проверке, прошлая попытка рядом, сдачи студента под календарём. Проверить на
телефоне у Умар устаза: развёрнутые чужие группы после проверки. Переснять
снимки устаза в разделе «Знания» через день-два (метка «через апп» ушла).

Поправка пользователя: раздела «Как работаем» больше нет — дверь «Знания»
(с 11.09), внутри блок «Как работать в приложении». Найдено при этом: тексты
бота новичку подготовительной (core/i18n.py:412, :439) всё ещё шлют в
«раздел «Как работаем»» — предложено поправить, ждёт решения.

## Правило подготовительной в приветствии + «Знания» в текстах бота

Решение пользователя: новичку сразу — 14 дней, 5 полных дней (все три
задания), переход сразу как наберётся. Вписано в оба приветствия в ГРУППЕ
(prep_onboarding_group_dm_needed/_dm_ready): до лички доходят не все.
Продление срока (+1 день за полный день) нарочно не называем — «14 дней»
честный минимум. Утренняя сводка: «нужно ≥5 дней» → «нужно 5 полных дней
(все три задания)», только ru (ky не ошибочна, пойдёт с локализацией).
«Как работаем» → «Знания» в prep_onboarding_intro и prep_onboarding_rules.
Тест: в обоих приветствиях есть «14 дней», «5 полных дней», «все три задания».
Коммит d9f4931, 302 теста. Выкладка: pull 07:05:58 UTC, мужской 07:05:59,
женский перезапущен руками 07:06:04 («Бот запущен: @yassirfemalebot»), оба active.

Осталось на сегодня: переснять снимки устаза в «Знаниях» (u_queue с ушедшей
меткой «через апп») — ждёт решения «сейчас или через день-два»; проверка на
телефоне у Умар устаза (развёрнутые чужие группы после проверки/«Дальше»).

## Вылезание листа у Муслима (Н-1, iPhone 14): фикс утра не помог

Снимки materials/айфон 1-3. По nginx: его Telegram в 06:12 UTC взял index.html
после ffd5d29 (text-size-adjust уже был) — значит, вылезание живёт на новой
версии. Стр. 51 влезает, стр. 6 (его место заучивания, 6/3/1 в проде) — шрифт
на глаз ~34 px, это потолок MUSHAF_MAX_FONT_SIZE, при норме 22.4. Chrome на iOS
и Telegram — один движок WebKit, «старый Chrome» ни при чём (CriOS 153).
Playwright WebKit iPhone 14, путь 51 → 📖 → стр. 6 → ✕ → перезапуск: 22.4 px,
ноль вылезаний — с машины не воспроизводится. Версия: в момент подгонки
строки ещё «короткие» (шрифт страницы не разложен), замер уходит в потолок,
самопроверка тоже не видит вылезания, позже никто не перепроверяет.
Предложено: перепроверка после отрисовки (кадры + 0.5/1.5 с, viewportChanged),
не верить потолку на обычной странице, отчёт на сервер при поздней поправке.
Ждёт решения.
Уточнения: это Муслим из Н-1. Пользователь «помог поменять место на 51/52» —
в базе место так и осталось 6/3/1 (01:33 UTC), POST /hifz с его адреса не было,
у других аккаунтов сегодня стр. 51/52 тоже нет. По логу он листал до стр. 52
внутри «поправить», но строку не выбрал, а место сохраняется только выбором
строки. Сдач из приложения у него нет — стена пересдачи ни при чём. Адрес
95.87.68.45 общий (LTE, тот же адрес ставил вердикт в 01:46).
Проблема одна и в Chrome, и в Telegram: на iOS оба — WebKit.
Требование пользователя: Safari всех айфонов с iPhone 11. Данные nginx: iOS
почти вся 17.3–26.6; пол приложения — iOS 16.2 (color-mix, 27 мест).
Первоисточник найден: WebKit bugs 174030 / 225790 / 217047 — document.fonts.ready
резолвится ДО раскладки, у Blink/Gecko — после первой раскладки. Это ровно
наша схема: reveal() → fonts.ready → clampFontToFit. На iOS замер видит текст
без шрифта страницы → ширины «короткие» → кегль в потолок 34, самопроверка тоже
до раскладки. На стенде не ловится: Playwright WebKit — не iOS-порт.

## Фикс: подгонка кегля повторами после раскладки (решение: «делай все три»)

1. `mushafFitSchedule()` вместо `document.fonts.ready.then(clampFontToFit)` в
   reveal() и openMushaf(): подгонка через два кадра, 500 мс и 1500 мс
   (`MUSHAF_REFIT_DELAYS`); номер прогона глушит повторы чужой страницы.
2. «Не верить потолку»: на листе ≥10 строк кегль = MUSHAF_MAX_FONT_SIZE
   считается ложным замером — держим `mushafFitTrusted` (последний полный
   лист не в потолке), последний повтор потолку верит.
3. Отчёт `POST /api/muf/fitlog` (тихий, `quiet: true` в apiCall — в счёт
   баннера «нет связи» не идёт), когда поздний повтор сдвинул кегль >0.5 px
   или первый замер упёрся в потолок. Сервер пишет строку в журнал:
   `journalctl -u yassir-bot | grep fitlog` — user, page, lines, avail,
   first/why[capped], final/why, iOS/Android + UA.

Проверки: 305 тестов (3 новых, tests/test_fitlog_api.py). Chrome 39 страниц —
те же 21.8–27.6 px, ноль вылезаний, что до правки. Playwright Chromium, стенд:
обычный режим — три подгонки на страницу ставят одно и то же число, отчётов 0;
имитация айфона (300 мс serif поверх .v4-word) — первый кегль 25–30, через
500 мс правильный, 4 отчёта; имитация «потолка» (font-size 1px) — лист держит
прошлый размер вместо 34, правится через 500–1500 мс, отчёты с capped.
Ответ пользователю на тревогу «а у Android»: ветки по браузеру нет
(решение 04.09), повторы одинаковы для всех, стоят ~1 мс каждый и меняют
число только там, где первый замер соврал.
Живой айфон по-прежнему не проверен — ждём журнал после выкладки и Муслима.
Коммит a5124a4. Выкладка: pull 07:38:35 UTC, мужской 07:38:37, женский
перезапущен руками 07:38:40, оба active; публичная страница отдаёт
mushafFitSchedule. Ждём: Муслим открывает заучивание на стр. 6 →
`journalctl -u yassir-bot | grep fitlog`.

## Египетская (книжная) разбивка — обсуждение, не сейчас

Вопрос пользователя: часть студентов любит египетскую разбивку, можно ли так
в приложении. Разобрано: нужны данные разбивки (слова по строкам каждого
листа) и шрифт; цветной таджвид есть только у мединского QPC V4.
Пользователь: египетский почерк без цветов — приемлемо. Устаз проверяет
то, что приходит (строка с переходным словом / полстраницы), «страница у
всех одинаковая». Проверить, когда дойдём: совпадают ли ГРАНИЦЫ листов и
номера строк египетского издания с мединскими — от этого зависит, говорят
ли студент и устаз «стр. 6, строка 4» об одном месте (место, сдачи, разбор,
снимки «Знаний»). Первым шагом — источник разбивки и шрифт.

## Таблица ширин листов — кегль до первого кадра (решение: «да, делай»)

Пользователь: чтобы при последующих открытиях страниц не было дёрганий.
Сделано: scripts/measure_mushaf_widths.py снимает естественную ширину самой
длинной строки каждого листа при 40px (Chromium, мерка clampFontToFit),
--apply вписывает MUSHAF_FIT_W в index.html между метками FIT_WIDTHS.
Приложение: mushafFitPredict ставит кегль сразу после вставки текста
(data-page на #ayah-text), ранние повторы его не трогают, поздний
(1500 мс) сверяет: согласен (≤0.5px и не вылезает) — число не меняется;
нет — правит и шлёт /fitlog. Коэффициент движка mushafFitK = замер/таблица,
0.9–1.1, в localStorage 'yassir_fit_k'. Без листа в таблице — прежняя схема.
Пробный прогон 7 листов совпал с живой подгонкой до 0.1px (стр. 6 — 22.4).
Замер 604 листов: 614 с, ширина при 40px 534–768 (стр. 2 рыхлая, стр. 328
плотная), кегль на 374px 19.2–27.6. Пробы на стенде (Chromium, iPhone-ширина):
normal / ios-sim (300 мс serif) / ios-cap (font-size 1px) — одна запись
кегля в первые ~100 мс, сверка в 1600 мс то же число, отчётов 0 (рывок после
утренней правки ушёл); engine-2pct — стр. 1 один раз 25.9→25.4 и отчёт,
дальше все листы сразу на 2% мельче без правок. 305 тестов.

Муслим и место: пользователь видел, что он тапал строку. POST /hifz с его
адреса нет вовсе. Код цепочки тап→hifzPickDone→hifzSave чист, touch-обработчики
пассивные (click не глушится), justSwiped на выбор не влияет, других
capture-перехватов нет. Лог одинаково объясняет второй сценарий: вышел из
заучивания, в ЧТЕНИИ долистал до 52 и тапнул (там тап место не ставит), 📖
снова увёл на стр. 6. Проверка пробой на стенде (WebKit) — в работе.

Египетская разбивка: QUL — 12 раскладок, египетской нет; Шамарли ~522 стр.
по 15 строк (первые две по 7) — нумерация с нашими 604 не совпадает;
открытый проект Шамарли на GitHub — страницы картинками из PDF, без
раскладки слов. Нужны фото у пользователя: разворот с номером, страница
начала 2:30 (у нас стр. 6), номер последней страницы.
Коммит ed8e793 (таблица ширин), выкладка запущена.

Проба «поправить → стр. 52 → тап» в Playwright WebKit iPhone 14: тап в режиме
выбора ДОХОДИТ до POST /hifz — путь исправен и на WebKit. Значит, у Муслима
тап был не в режиме выбора (скорее всего в обычном чтении). Попутно
найдена настоящая ошибка: после ввода номера страницы человек убирает
клавиатуру тапом по листу — этот тап засчитывается как выбор строки: blur →
loadPage ставит currentPage=52 синхронно, а под пальцем ещё текст стр. 6, и
уходит POST {page:52, line:0}. Место встаёт на строку, которую человек не
выбирал. Фикс-кандидат: принимать тап выбора только если data-page текста
равен currentPage (лист уже перерисован). Ждёт решения. (Дальше проба
упёрлась в стенд: у студента стенда долг пересдачи → 409 → openSubs.)
Выкладка ed8e793: pull 08:32:05 UTC, мужской 08:32:07, женский перезапущен
08:32:17, оба active; публичная страница отдаёт MUSHAF_FIT_W. Записей fitlog
за 07:38–08:32 (схема с повторами) — ни одной.

## Фикс «поправить»: тап-закрытие клавиатуры ставил место (решение: «делай»)

Прод перед этим проверен по живому адресу: yassirilm.com байт в байт = локальный
index.html (589 627), MUSHAF_FIT_W и mushafFitVerify на месте, /fitlog 401 на
обоих ботах (маршрут есть), ETag/Last-Modified = 08:32:07.
Правка: в обработчике тапа выбора строки — выход, если data-page текста ≠
currentPage. Текст листа вставляет только reveal() (grep: одно место), он же
ставит data-page — тап отклоняется лишь пока новый лист не нарисован.
Стенд WebKit/Chromium: закрытие клавиатуры тапом — POST нет; тап строки 4 на
стр. 52 — POST {page:52,line:3}; свайпы (Chromium) — то же. WebKit-свайп не
прогнан: Playwright WebKit не создаёт TouchEvent — ограничение пробы.
Ответ сервера в пробе — 409 retake_pending (у студента стенда долг стр. 3,
строка 3): указатель на стенде не двигается из-за стены пересдачи, не из-за
фикса. У Муслима сдач из приложения нет. Коммит 18b0f44, выкладка запущена.
Выкладка 18b0f44 НЕ прошла с первого раза: Actions — check (305 тестов) зелёный,
deploy красный. Аннотация GitHub: «The job was not started because it
repeatedly failed to be acquired (5 attempts)» — раннер не выдан, шагов 0,
до сервера дело не дошло (сбой на стороне GitHub, не код и не сервер). Моя
фоновая проверка ждала 10 минут и честно показала head=ed8e793, «live fix: 0».
Перезапуск упавшего задания: gh run rerun 34748401725 --failed — ждём.
Перезапуск задания — success. Выкладка 18b0f44: pull 08:56:16 UTC, мужской
08:56:18, женский перезапущен 08:56:35, оба active; публичная страница отдаёт
проверку data-page в тапе выбора (live fix: 1).

## Живой айфон: таблица верна, замер врёт и после раскладки

Муслим открыл 15:25 (Safari 18.7.5, IP сменился). Отчёты fitlog: стр. 51 —
table 22.3 → сверка 34.0 (потолок); стр. 5 — 22.0 → 34.0; стр. 6 — 22.4 →
34.0. То есть после ed8e793 сверка ПЕРЕБИВАЛА верную таблицу — текст за
краем на КАЖДОЙ странице (сделал хуже утреннего). Версия про
document.fonts.ready для этого телефона неверна: scrollWidth строк врёт
и через 1.5 с. Фикс 714a802: замер вне ±10% от таблицы — сломанный,
кегль остаётся табличным, отчёт уносит три мерки (table / scroll / rect по
getBoundingClientRect). Стенд: scrollWidth=clientWidth → таблица держится,
отчёты scroll=374 rect=таблица; normal и +2% без изменений. 306 тестов.
Выкладка: pull 09:35:25 UTC, мужской 09:35:26, женский перезапущен, live ок.
Ждём второго захода Муслима: по rect в журнале станет ясно, какая мерка врёт.
Место в базе всё ещё 6/3/1.

Коридор ±10%: разница движков 1–2% (на стенде +2% дал k=1.019), враньё
айфона — 43% (k≈0.57). Порог посередине с запасом в обе стороны; вопрос
пользователя «почему 10, не 5 и не больше» — ответ: любое число между
~3 и ~30 работает одинаково, 10 — не тонкая настройка.

## «Мой день» в шапке — без числа (решение: «да, убирай везде»)

Жалоба: «6 из 6» / «0 из N» не помещается, подписи заданий и так внизу.
Снимки на стенде (logs/dash_variants/myday_compare.jpg): при 6 заданиях с
числом подписи слипаются, без числа — свободно; при 3 число не нужно.
Убрано везде: `#dash-head #my-day .cap { display: none }`, сетка в одну
колонку. Проверено снимком с настоящим CSS (myday_after.jpg). Разметка .cap
оставлена — карточка «Мой день» вне шапки (старая раскладка) её использует.
Снимок дашборда в «Знаниях» (onboarding/dash.jpg) и так устарел с 10.09 —
переснять вместе с устазскими.
Коммит 5fe1cd5. Выкладка: pull 09:40:41 UTC, мужской 09:40:42, женский
перезапущен, оба active, live отдаёт правило. Новых fitlog после 09:35 нет —
Муслим после защитного фикса ещё не заходил.

## Второй заход Муслима (15:41–15:44): размер норм, место не сохраняется

Шрифт: 8 отчётов, таблица держится (first=22.x, сверка 34 «bad» отброшена).
Обе мерки врут одинаково: scroll=374, rect=368–369 = ровно таблица × кегль/40
(659×22.4/40=369.0; 649×22.7/40=368.3). Две гипотезы, одно число не
различает: (а) 40px до раскладки не доходит, меряем при 22.4; (б) слова
ужаты в контейнер вопреки flex: 0 0 auto. transition/will-change в CSS
листа нет. Добавлены диагностики в отчёт: fs (computed font-size в момент
замера), w1/w1n (ширина первого слова при 40px и при обычном кегле).
Ещё не выложено.

Место: POST /hifz с его телефона — ни одного, 4xx нет. Путь «поправить →
51 → «Половина» → тап по строке» на стенде WebKit даёт POST {51,7,2} — код
исправен. Значит, тапа по половине не было. Найдено в hifzHead: когда лист
≠ листу указателя, подсказка «тапни по половине» ЗАМЕНЯЕТСЯ на «твоё место
осталось на стр. 6» — человек на стр. 51 после «Половина» видит «Верхняя или
нижняя?» и про тап не узнаёт; ✕ выходит молча, ничего не сохранив. Его
хронология: 📖 (стр. 6) → «поправить» → ›×44 до 50 → 52 → 53 → 51 → ✕ →
📖 → снова стр. 6. Предложение пользователю: держать «тапни по половине /
строке» всегда, «место осталось на стр. N» второй строкой; и/или на ✕ во
время выбора — короткое «место не изменено».

## Ибрахим (Н-1): голосовое в группе сдвинуло место на вторую половину

Вопрос Умар устаза. Данные: указатель 13/7/1 (вторая половина, строка 1,
этап 1), updated_at 09:41:53 UTC. В журнале ровно в 09:41:53 — голосовое
Ибрахима в группе Н-1 (пересдача вчерашней строки по замечанию). handlers.py:
1840 — advance_hifz_pointer на ЛЮБОЕ голосовое в группе (решение 02.09,
6c1c5be: «указатель двигают оба канала»). Указатель стоял на 13/0/2 (первая
половина, этап 2, сдана из приложения 05:12 и принята) → next → 13/7/1.
Принятие Умаром ни при чём — сдвиг случился в момент отправки голосового.
Вернуться можно самому: 📖 → «поправить» → «Половина» → тап по верхней
половине (сервер пустит: открытых пересдач нет). Базу руками не трогаем.
Предложение: голосовое в группе не двигает указатель, если у студента есть
сдачи из приложения — его голосовые в группе это ответы устазу, не новые
единицы. Ждёт решения.

## Три правки места (решения: «то что в группе сдает, конечно», «2 ок», «3 тоже»)

1. handlers.py: advance_hifz_pointer на голосовое в группе — только если
   has_app_submissions(student) == False (новый helper в db.py, тест
   tests/test_group_voice_pointer.py).
2. hifzHead: подсказка «тапни по строке/половине» всегда; на чужой странице
   к ней « · твоё место осталось на стр. N».
3. hifzExit посреди выбора → плашка #hifz-note «Место не изменено» (2.5 с);
   строка hifz_pick_cancel, только ru.
Стенд WebKit: подсказка обе строки, плашка видна и уходит, место не тронуто;
снимок проверен глазами. 308 тестов. Замечено: после перехода на другую
страницу в режиме строки sub не обновляется до тапа по единице (hifzHead не
зовётся при рендере в pick) — мелочь, оставлено.
Ибрахиму: 📖 → «поправить» → «Половина» → верхняя половина.
Коммит 9aefec3. Actions задержался (~5 мин до старта), затем success.
Выкладка: pull ~10:19:5x UTC, мужской 10:19:55, женский перезапущен 10:20:04,
оба active; yassirilm.com байт в байт = локальный index.html, has_app_submissions
на сервере в handlers.py и db.py.

## Фиксация сессии (вечер)

context/summary.md — новый блок «Закрыто 13.09 (день–вечер)» и открытые
пункты; wiki/mushaf_yassirapp.md — разделы про fonts.ready, таблицу ширин,
указатель и голосовое в группе, подсказки «поправить». Egyptian: Шамарли
522 стр., ждём 3 фото от Нура. Замечание про «просторнее» взято назад:
522 против 604 при тех же 15 строках — текста на странице больше.

## «След» действий (решение: «давай сейчас»)

core/app_trail.py + app_trail в hadiths.db (3 дня, чистка при записи), пачка
в теле heartbeat (без нового запроса), 10 событий в index.html,
scripts/user_trail.py — лента по времени из следа, сдач, fitlog, места.
Уточнение пользователя учтено: сдачи/место/страницы/fitlog не дублируем.
Стенд WebKit: путь Муслима читается строка в строку. 312 тестов.
Выкладка 049177c: pull 10:39:48 UTC, мужской 10:39:49, женский перезапущен,
оба active, live отдаёт trailBuf. scripts/user_trail.py на живых данных
(Муслим, 6 ч): 12 строк fitlog + текущее место; след пуст — телефон ещё не
брал новую версию. Первый живой след появится при следующем открытии.
